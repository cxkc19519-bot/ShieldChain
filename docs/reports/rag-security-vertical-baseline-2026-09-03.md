# 安全垂直知识库 RAG 固定基线（2026-09-03）

## 结论

`shieldchain-security-vertical-v1` 已能通过真实本地 RAG 评测接口执行，但本次离线降级基线**未通过质量门禁**。这是可信的失败结果：运行时没有启动 BGE-M3、Milvus 和 BGE Reranker，系统只使用 BM25，并明确记录 2 次能力探测均失败；不得将本报告解释为完整本地模型链路的验收结果。

## 固定输入

- 知识包：`shieldchain-security-vertical` 2026.09.3；
- 资料：13 份（5 份项目摘要、4 份深信服官方 PDF、4 份中央网信办官方 HTML）；
- 评测集：`sample_docs/security_vertical/evaluation/shieldchain-security-vertical-v1.json`；
- 评测集版本：1.0.0；
- 评测集 SHA-256：`657aab283a2dc366c9fc2da5869923050daf658d093576f8c521771391534d05`；
- 用例：12 条，覆盖中文/英文、法规、事件报告、金融数据分类、0day、ATT&CK、深信服研究、跨语言检索与无依据拒答；
- K：5；
- 外部网络/云费用：无，估算费用 0 美元。

## 结果

| 指标 | 实测 | 门槛 | 结论 |
| --- | ---: | ---: | --- |
| Recall@5 | 0.7000 | >= 0.7500 | 未通过 |
| MRR@5 | 0.6333 | >= 0.7000 | 未通过 |
| nDCG@5 | 0.6103 | >= 0.7000 | 未通过 |
| 引用正确率 | 0.2667 | >= 0.8000 | 未通过 |
| 引用精确率 | 0.3667 | >= 0.8000 | 未通过 |
| 期望引用召回率 | 0.8000 | >= 0.9000 | 未通过 |
| 抽取式忠实度 | 1.0000 | >= 1.0000 | 通过 |
| 拒答准确率 | 1.0000 | >= 0.9000 | 通过 |
| 失败率 | 1.0000（2/2） | <= 0.0500 | 未通过 |
| BM25 基线 nDCG@5 | 0.6168 | 仅记录 | - |
| 重排增益 | -0.0066 | 仅记录 | 未启动真实重排，不作效果结论 |

质量门禁：**未通过**。

## 性能记录

- 13 份资料首次导入及规则降级分块：39,775.863 ms；
- 12 条固定用例评测总耗时：4,193.903 ms；
- 单条查询 P50：2.035 ms；
- 单条查询 P95：1,846.115 ms。

P95 主要由首个用例探测未启动的向量与重排服务产生。评测运行内加入能力熔断：首次确定服务不可用后不再对剩余用例重复等待，但失败率仍按真实尝试记录，门禁保持失败。

## 逐题诊断摘要

- 通过：2/12，均为不存在知识的中英文拒答题；
- 未通过：10/12，均为可回答检索题；
- 缺少期望相关文档：5 题；
- 引用了期望集合外的文档：10 题；
- 缺少指定引用：2 题；
- 检索依赖失败：1 题（首题完成真实探测，随后熔断）；
- 无证据支撑的抽取内容：0 题。

逐题结果已由评测 API 返回并在知识库页面展开显示，包括查询、期望文档、BM25 基线、最终命中、实际引用、拒答状态、分项分数和失败原因。这里的“抽取式忠实度 1.0”只表示返回的答案片段可在本次引用证据中逐字定位；它不等于 Qwen 生成答案的语义事实正确率，也不证明检索到的文档就是正确文档。

## 已发现问题与下一步

1. 启动 BGE-M3、Milvus 和 BGE Reranker 后，用同一数据集复跑完整链路，生成独立报告，不能覆盖本降级基线。
2. 当前抽取式响应会展示并引用全部入选片段，虽然不会脱离证据生成内容，但引用精确率偏低；需要增加证据选择，让回答只保留实际支持问题结论的片段。
3. 英文问题检索中文法规正文依赖跨语言向量能力；纯 BM25 结果只能作为降级基线。
4. 完成本地 Qwen 生成链路后，另行增加语义声明级支持率与事实忠实度评测；当前结果只证明真实检索、抽取式答案和可审计诊断已接通。
