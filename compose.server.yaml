services:
  endpoint-response-executor:
    build:
      context: .
      dockerfile: backend/Dockerfile.endpoint-executor
      args:
        BASE_IMAGE: ${ENDPOINT_EXECUTOR_BASE_IMAGE:-shieldchain-response-executor:local}
    image: shieldchain-endpoint-response-executor:local
    network_mode: "container:${WAZUH_AGENT_CONTAINER:-shieldchain-demo-agent-wazuh.agent-1}"
    environment:
      SHIELDCHAIN_ENDPOINT_EXECUTOR_TOKEN: ${RESPONSE_FIREWALL_EXECUTOR_TOKEN:-}
      SHIELDCHAIN_ENDPOINT_EXECUTOR_SOCKET: /run/shieldchain-endpoint-executor/executor.sock
      SHIELDCHAIN_ENDPOINT_AGENT_ID: ${RESPONSE_WAZUH_ALLOWED_AGENT_IDS:-002}
      SHIELDCHAIN_FILE_LAB_ROOT: /var/lib/shieldchain-file-lab
      SHIELDCHAIN_ALLOWED_FILE_IDS: ${RESPONSE_ALLOWED_FILE_IDS:-demo-suspicious-marker}
    volumes:
      - shieldchain-endpoint-executor-socket:/run/shieldchain-endpoint-executor
      - shieldchain-file-response-lab:/var/lib/shieldchain-file-lab
    read_only: true
    cap_drop:
      - ALL
    cap_add:
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    pids_limit: 64
    tmpfs:
      - /tmp:size=8m,mode=1777
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import socket; s=socket.socket(socket.AF_UNIX); s.settimeout(2); s.connect('/run/shieldchain-endpoint-executor/executor.sock'); s.close()"
      interval: 10s
      timeout: 3s
      start_period: 5s
      retries: 6
    restart: unless-stopped

  wazuh-response-executor:
    build:
      context: .
      dockerfile: backend/Dockerfile.wazuh-executor
    image: shieldchain-wazuh-response-executor:local
    network_mode: host
    env_file:
      - ${WAZUH_ENV_FILE:-./secrets/wazuh-readonly.env}
    environment:
      SHIELDCHAIN_WAZUH_EXECUTOR_TOKEN: ${RESPONSE_FIREWALL_EXECUTOR_TOKEN:-}
      SHIELDCHAIN_WAZUH_EXECUTOR_SOCKET: /run/shieldchain-wazuh-executor/executor.sock
      SHIELDCHAIN_ENDPOINT_EXECUTOR_SOCKET: /run/shieldchain-endpoint-executor/executor.sock
      SHIELDCHAIN_WAZUH_ALLOWED_AGENT_IDS: ${RESPONSE_WAZUH_ALLOWED_AGENT_IDS:-002}
      WAZUH_API_URL: ${WAZUH_API_URL:-https://127.0.0.1:55000}
      WAZUH_API_TLS_VERIFY: ${WAZUH_API_TLS_VERIFY:-false}
    volumes:
      - shieldchain-wazuh-executor-socket:/run/shieldchain-wazuh-executor
      - shieldchain-endpoint-executor-socket:/run/shieldchain-endpoint-executor
    read_only: true
    user: "10001:10001"
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 64
    tmpfs:
      - /tmp:size=8m,mode=1777
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import socket; s=socket.socket(socket.AF_UNIX); s.settimeout(2); s.connect('/run/shieldchain-wazuh-executor/executor.sock'); s.close()"
      interval: 10s
      timeout: 3s
      start_period: 5s
      retries: 6
    restart: unless-stopped
    depends_on:
      endpoint-response-executor:
        condition: service_healthy

  response-executor:
    build:
      context: .
      dockerfile: backend/Dockerfile.executor
    image: shieldchain-response-executor:local
    network_mode: host
    environment:
      SHIELDCHAIN_FIREWALL_EXECUTOR_TOKEN: ${RESPONSE_FIREWALL_EXECUTOR_TOKEN:-}
      SHIELDCHAIN_FIREWALL_EXECUTOR_SOCKET: /run/shieldchain-executor/executor.sock
      SHIELDCHAIN_FIREWALL_ALLOWED_CIDRS: ${RESPONSE_FIREWALL_ALLOWED_CIDRS:-192.0.2.0/24,198.51.100.0/24,203.0.113.0/24}
    volumes:
      - shieldchain-executor-socket:/run/shieldchain-executor
    read_only: true
    cap_drop:
      - ALL
    cap_add:
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    pids_limit: 64
    tmpfs:
      - /tmp:size=8m,mode=1777
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import socket; s=socket.socket(socket.AF_UNIX); s.settimeout(2); s.connect('/run/shieldchain-executor/executor.sock'); s.close()"
      interval: 10s
      timeout: 3s
      start_period: 5s
      retries: 6
    restart: unless-stopped

  backend:
    env_file:
      - .env
    environment:
      SHIELDCHAIN_LOCAL_KNOWLEDGE_ROOT: /var/lib/shieldchain/local-knowledge
      SHIELDCHAIN_LOCAL_RAG_URL: http://shieldchain-rag-models:8000
      SHIELDCHAIN_LOCAL_MILVUS_URI: http://shieldchain-milvus:19530
      ASSISTANT_DATA_ROOT: /var/lib/shieldchain/assistant
      RESPONSE_WAZUH_CONNECTOR_ENABLED: "true"
      WAZUH_AUTO_INVESTIGATION_ENABLED: "true"
    depends_on:
      response-executor:
        condition: service_healthy
      wazuh-response-executor:
        condition: service_healthy

  frontend:
    pids_limit: 256
