# 2026-07-22 开发日志

## 已完成

- 完成阶段 4 Task 4：新增服务端可信访问上下文、默认拒绝的智能体内容权限矩阵、租户/敏感级别/权限标签检查和字段 allowlist 投影。
- 私有上下文只允许 owner 角色读取；结构化交接只允许发送方、接收方和超级智能体读取；未知内容类型和无权限组合均 fail closed。
- 投影递归移除客户端或模型载荷中的 tenant、principal、原始提示和思维链字段，防止它们覆盖服务端权限边界。
- 新增证据、知识、用户输入、工具结果和交接的不可信 JSON 包络；提示注入样本只能保留为数据，并带确定性检测标记。
- 扩展结构化日志脱敏，除敏感键外同步清理字符串中的 Bearer 凭据和常见 `api_key/token/password/secret` 赋值形式。

## 修改文件

- `backend/src/shieldchain/agents/security.py`
- `backend/src/shieldchain/core/logging.py`
- `backend/tests/unit/agents/test_security.py`
- `backend/tests/unit/agents/test_security_matrix.py`
- `backend/tests/unit/core/test_logging_security.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-22.md`

## 验证

- Task 4 聚焦测试：`23 passed`；扩展矩阵覆盖 7 类内容 × 7 个智能体角色。
- 智能体、核心安全、RAG 权限相关回归：`94 passed`。
- 完整后端 Ruff：`All checks passed`。
- 完整后端 Pytest：`738 passed, 1 skipped`。
- 初次完整 Pytest 的唯一失败来自 Git dubious ownership；重跑时仅通过进程环境提供当前工作树的 `safe.directory`，未修改全局 Git 配置，门禁通过。

## 遗留问题与风险

- 当前工作树 `.venv` 的 editable install 仍指向旧的阶段 2 工作树；本次验证显式设置 `PYTHONPATH=backend/src` 使用当前源码。后续重新创建或重新安装该工作树虚拟环境时应消除此环境漂移。
- Pytest 缓存目录在当前 Windows 工作树上出现非阻断写入警告，不影响测试结果，但后续完整脚本门禁应继续观察。
- Task 4 只提供确定性安全原语，尚未装配提示上下文，也没有引入阶段 5 工具执行能力。

## 阶段 4 Task 5

- 新增固定顺序上下文装配器：系统规则、安全边界、当前任务、允许动作、案件摘要、证据、知识、交接和输出 Schema。
- 所有案件与候选内容先经过 Task 4 权限投影，再执行相关性、时间衰减、稳定排序和去重；未授权内容不进入排序且不泄露来源。
- 重复内容合并后保留全部来源 ID；证据、知识与交接始终使用结构化不可信 JSON 包络，提示注入只作为数据和检测标记保留。
- 系统规则、安全边界和输出 Schema 属于不可截断区；必要上下文超预算时显式拒绝，可选项超预算时按完整项省略并返回降级原因。
- 候选载荷递归冻结且只接受 JSON-safe 值；案件摘要再次移除 tenant、principal、原始提示和思维链字段。
- 新增 `backend/src/shieldchain/agents/context.py`、`test_context_assembly.py` 和 `test_context_assembly_security.py`。
- Task 5 聚焦测试 `13 passed`；智能体、核心安全和上下文仓储相关回归 `118 passed`；Ruff 全绿。

### Task 5 风险

- Token 估算采用确定性 UTF-8 字节近似，不代表特定云模型 tokenizer；后续接入真实模型时必须通过适配器提供更精确但同样受硬上限约束的计数器。
- 本任务只装配结构化上下文，不调用 LLM、不生成工具权限，也不实现长期记忆写入。

## 阶段 4 Task 6

- 新增工作、案件、会话、审计和经验五层记忆边界；工作记忆必须过期，案件/审计记忆必须绑定案件，会话记忆禁止携带安全证据引用。
- 审计记忆拒绝原始提示和思维链；经验记忆禁止直接构造，必须通过人工确认与脱敏后的推广服务进入长期知识端口。
- 安全压缩仅截断叙述摘要，结构化确认事实、事实引用及证据/审批/工具结果的不可变引用始终独立保留。
- 过期假设退出活动记忆但进入归档集合，继续可供审计；跨案件引用和伪造引用类型默认拒绝。
- 新增 `backend/src/shieldchain/agents/memory.py` 和 `backend/tests/unit/agents/test_memory.py`。
- Task 6 聚焦测试 `10 passed`；全部智能体单元与集成回归 `122 passed`；Ruff 全绿。

### Task 6 风险

- 本任务定义确定性记忆领域边界、压缩和长期知识端口，尚未新增独立记忆数据库表；案件事实和交接仍复用 Task 2–3 持久化。
- 不调用云服务，不把自动生成经验写入长期知识库。

## 下一步

- 开始阶段 4 Task 7：实现结构化交接服务、同案件引用重解析和只追加持久化。
