# 2026-09-03 开发日志

## 安全垂直知识库：中央网信办官方页面本地化

- 按协作要求，将《网络数据安全管理条例》《国家网络安全事件报告管理办法》《金融信息服务数据分类分级指南》答记者问，以及国家网信办行政法规目录保存为项目内 UTF-8 HTML 快照。
- 知识包升级为 `2026.09.3`，清单登记 4 份 HTML 的官方来源、发布/访问日期、来源等级、权限标签、复核窗口与 SHA-256。知识包现有 13 份本地资料：5 份项目摘要、4 份深信服官方 PDF、4 份中央网信办官方 HTML。
- `01_中国网络与数据安全合规控制基线_2026-07.md` 第 10 节改为本地文件清单；RAG 运行时读取本地文件，官网地址只用于溯源、更新检查和重新下载。动态行政法规目录明确标记为访问日快照，不能替代具体法规正文。
- 受管知识包校验新增 `text/html`：限制为当前目录直接文件，校验 UTF-8、HTML 基本结构、媒体类型、文件大小和 SHA-256；任一文件失败仍整包拒绝。
- 本地服务和确定性解析器优先读取 `#BodyLabel`、`.main-content` 或 `.side-right-column`，并移除脚本、样式、导航及页脚。实际快照正文提取字符数为 4,070、9,988、398、1,633，均未混入“设为首页”等导航文本。

## 验证

- 后端知识包、解析器和知识 API 定向回归：60 项通过。
- 后端 RAG、助手和知识 API 扩大回归：318 项通过，1 项显式跳过。
- 前端知识库页面/API：10 项通过；TypeScript 类型检查与 ESLint 通过。
- Ruff 与 `git diff --check` 通过；后者仅提示现有 Windows CRLF 转换策略。

## 边界

- 本次没有执行提交、推送或部署，也没有操作生产数据。
- HTML 是有日期的离线快照，不会自动跟随官网更新；仍需按清单复核期限重新下载、审阅差异并更新哈希。

## RAG 固定评测与质量门禁

- 对照比赛任务书确认：高质量安全垂直知识库的可运行、可演示、可审计交付已经完成，专家签字与自动更新属于持续运营缺口；开始推进下一项 RAG。
- 新增 `shieldchain-security-vertical-v1` 1.0.0 固定集，共 12 条中英文用例，相关证据标识直接绑定受管知识包文件名。
- 修复默认本地知识服务 `evaluate` 只返回 0 条空指标且缺少 `principal_id` 的接口问题；评测现在对显式选择的知识库执行真实 BM25、向量、重排链路，计算质量、性能、调用和失败指标，并按固定阈值失败关闭。
- 前端不再请求不存在的 `phase3-security-baseline`，改用真实数据集并发送当前知识库 ID，避免跨库混入证据。
- 批量评测预计算一次 BM25 语料统计，并在本轮首次确认向量/重排不可用后停止重复探测。完整 12 题降级评测由约 45 秒降为 4,169 ms，单题 P50 为 2.275 ms；首次导入 13 份资料耗时 39,333 ms。
- 离线 BM25 基线：Recall@5 0.70、MRR@5 0.6333、nDCG@5 0.6103、引用正确率 0.2667、拒答准确率 1.0、失败率 1.0，门禁按设计未通过。报告保存为 `docs/reports/rag-security-vertical-baseline-2026-09-03.md`。
- RAG、助手、配置、主应用与知识 API 扩大回归：340 项通过、1 项显式跳过；前端知识库专项 12 项通过，TypeScript、ESLint、Ruff 和差异检查通过。

## RAG 抽取忠实度与逐题诊断

- 本地检索回答改为由实际命中片段组成的抽取式答案，明确标注“仅作为分析依据，不能授权工具执行”；不再返回只有“找到若干片段”的占位文本。
- 固定评测新增引用精确率、期望引用召回率和抽取式忠实度，并将阈值与逐题结果返回前端；逐题可查看期望文档、BM25 基线、最终命中、实际引用、拒答状态和失败原因。
- 完整 12 题降级基线复跑：数据集 SHA-256 为 `657aab283a2dc366c9fc2da5869923050daf658d093576f8c521771391534d05`；引用精确率 0.3667、期望引用召回率 0.80、抽取式忠实度 1.0，2/12 拒答题通过，整体门禁仍失败。
- 首次导入 13 份资料耗时 39,775.863 ms，评测耗时 4,193.903 ms，P50 2.035 ms，P95 1,846.115 ms；2 次本地向量/重排依赖探测失败后熔断。
- 抽取式忠实度只证明答案片段来自引用证据，不代表引用文档一定相关，也不替代未来 Qwen 生成答案的语义事实核验。

## 有依据的智能助手

- 修复助手生成提示中“要求输出引用编号”与“禁止并删除引用编号”的冲突，统一为纯文本正文与界面独立证据卡片。
- 助手引用新增并持久化知识库、文档、文档版本、内容块、标题路径、页码/结构位置、更新时间和 SHA-256；旧对话缺失字段时仍可按 `legacy` 状态读取。
- 助手消息新增 `conversational`、`grounded`、`extractive_degraded`、`refused` 等依据状态，以及拒答原因和检索/生成降级列表。
- 生成模型不可用但已经命中证据时，不再返回 503 丢弃本轮结果；改为最多直接展示 3 条证据片段、标记 `generation_degraded` 并要求人工复核后再行动。
- 前端现在实际展示引用证据和降级状态，可展开核对版本、块、页码/位置和完整性哈希。
- 新增有依据回答溯源持久化、生成失败抽取降级和前端引用展示测试；定向验证为后端 6 项、前端 2 项通过。
- 权威知识包导入进一步持久化来源等级/URL、核验日期和文档复核截止日期；这些字段随检索命中进入助手引用并在页面显示。
- 本地检索会排除超过复核截止日期的受管文档；当查询只能匹配过期资料时结构化拒答为 `stale_evidence`。新增定向测试后，知识包、RAG、助手和知识 API 共 38 项通过，前端知识/助手共 14 项通过。
- 最终扩大回归：后端 RAG、助手、配置与知识 API 343 项通过、1 项显式跳过；前端全量 119 项通过；TypeScript、ESLint、Ruff 与 `git diff --check` 通过（仅保留 Git 的 CRLF 转换提示）。
