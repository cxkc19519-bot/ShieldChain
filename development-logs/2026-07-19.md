# 2026-07-19 开发日志

## 已完成

- 完成阶段三 Task 6：DeepSeek 只返回严格 JSON 候选边界，本地确定性代码负责重建文本、哈希、稳定 ID、来源、页码、标题路径和 ACL，模型不能改写原文或元数据。
- 对遗漏、重复、越界、乱序、空边界、额外字段、重复 JSON key、Markdown 包裹、NaN、超限、来源重叠、重复输出 ID、伪造文本/哈希/Token/来源/权限等结果执行默认拒绝。
- DeepSeek 超时、限流、认证、不可用、网络连接失败、畸形响应和边界质检失败均回退 `rule_degraded`；网络重试有界，错误类别固定且不保存异常敏感文本。
- 统一语义策略与实际 LLM 客户端模型来源；拒绝模型配置不一致、空白模型名、空白响应及 JSON 解码递归错误。
- 新增迁移 `20260718_03`，持久化策略、提示词、请求/响应模型、失败类别和 canonical retry key；失败类别有应用层及数据库约束。
- 新增安全的首次 semantic 版本创建入口：使用真实 rule result 与 semantic result 共同重放并校验，支持两候选合并为一块，拒绝伪造 rule input。
- 新增 tenant-scoped 原子 rule→semantic 升级：数据库内重放边界并精确比较输出，重算 retry key，重复调用精确 no-op，异常完整回滚。
- PostgreSQL 锁顺序统一为 document → version，并用 `FOR UPDATE OF knowledge_documents` 契约测试固定；删除中、索引处理中/已成功或存在外部索引引用时拒绝升级，避免复活或遗留 Milvus/BM25 数据。
- 修复测试日志全局配置泄漏：每个测试后恢复 structlog 默认配置，避免捕获流关闭后污染后续测试。

## 验证

- Task 6 关键链路聚焦回归：`117 passed`；最后两项模型/畸形响应修复回归：`69 passed`。
- 最终全后端门禁：`474 passed, 1 skipped`。
- Ruff：通过；`git diff --check`：通过（仅 Windows LF/CRLF 提示）。

## 待办

- 提交 Task 6，并更新阶段进度账本。
- 进入阶段三 Task 7：BM25、BGE-M3 Embedding、托管 Milvus 适配边界与索引生命周期。
- 后续增加不可变的 chunking-attempt 审计表，长期保留每次失败、response model 和 Token usage；当前版本表保存当前有效状态。
