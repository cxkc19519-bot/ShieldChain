# 2026-07-23 开发日志

## 阶段 4 Task 8

- 新增七类专业角色统一请求、结果和注册表协议；角色输入只接受 Task 5 生成的 `AssembledContext`。
- 告警研判和威胁调查复用阶段 2 确定性钓鱼规则及可信证据适配端口，证据超时和不可用显式传播。
- 知识检索角色直接调用阶段 3 RAG 领域端口，只接受 `GroundedAnswer` 或 `StructuredRefusal`；引用转换为同案件 `KnowledgeReference`。
- 响应规划角色只输出 `proposed:` 动作且不持有工具端口；验证和报告角色在缺少可信事实时拒绝执行。
- 新增可插拔严格模型端口和确定性离线 fake；未知字段、畸形 Schema、超时和依赖失败不会包装成成功。
- 通用模型适配器禁止替换知识检索角色；用于响应规划时会强制把动作降为 `proposed:`。

## 修改文件

- `backend/src/shieldchain/agents/roles.py`
- `backend/tests/unit/agents/test_roles.py`
- `backend/tests/unit/agents/test_roles_security.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## 验证

- Task 8 聚焦测试：`10 passed`。
- 智能体、事件规则和 RAG 相关回归：`285 passed`。
- Ruff：`All checks passed`。
- 测试全程离线，不调用 DeepSeek、Embedding、Milvus 或 Reranker。

## 风险与遗留

- 默认角色实现是确定性离线实现；真实模型和真实 RAG 服务仍需在获得授权后通过现有端口接线。
- 角色只生成分析结果和 proposed actions，尚未实现 Task 9 编排状态机，也没有阶段 5 工具执行权限。
- 当前 Windows 工作树仍有非阻断 Pytest 缓存写入警告；测试结果不受影响。

## 阶段 4 Task 9

- 新增超级智能体白名单状态机，按研判、调查、可选检索、响应规划、可信执行边界、验证和报告的固定路径推进。
- 新增确定性安全监督器，在角色调用前检查步骤与 token 预算，并把拒绝、超时、依赖不可用、证据不足和无效输出映射为稳定原因码。
- 响应规划只进入 `awaiting_trusted_execution`，编排器不持有工具端口；高风险 proposed action 只能等待后续可信执行确认。
- 每一步把角色输出、结构化 handoff、审计事件和预算快照组成单个原子提交包；提交失败保留原状态并允许安全重试。
- 可信执行恢复必须显式传入已验证结果，否则转入人工复核；不存在自由循环或未计费重规划。

## Task 9 修改文件

- `backend/src/shieldchain/agents/orchestrator.py`
- `backend/tests/unit/agents/test_orchestrator.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 9 验证

- 聚焦测试：`6 passed`。
- 智能体单元与集成回归：`144 passed`。
- Ruff：`All checks passed`。
- 测试全程离线；未引入工具执行、云模型或外部检索调用。

## Task 9 风险与遗留

- 原子提交通过端口契约表达，SQLite 持久化与只读协作轨迹将在 Task 10 接线。
- 当前 Windows 工作树仍有非阻断 Pytest 缓存写入警告；测试结果不受影响。

## 阶段 4 Task 10

- 新增 `GET /api/v1/agents/runs/{run_id}/trajectory` 只读 API，使用服务端可信租户边界查询共享上下文、角色执行、交接和可信引用。
- 公开响应只包含共享摘要、已确认事实、角色状态、交接、引用、预算和原因码；查询层从不读取私有上下文表。
- 安全测试验证跨租户返回不存在，且响应不包含 tenant/principal、私有上下文、操作员目标、原始提示或思维链。
- 将 `/agents` 从占位页替换为最小只读工作台，支持按调查运行 ID 查看协作状态、预算、原因码、交接和引用。
- 新增阶段 4 smoke：在临时 SQLite 迁移到 head 后，用离线角色执行确定性编排并原子保存输出、handoff 和审计，再验证公开轨迹及泄漏边界。
- 完整验证脚本升级为阶段 4 门禁，同时保留阶段 3 RAG smoke 前置；脚本合同同步更新。

## Task 10 修改文件

- `backend/src/shieldchain/agents/schemas.py`
- `backend/src/shieldchain/agents/trajectory.py`
- `backend/src/shieldchain/api/agents.py`
- `backend/src/shieldchain/main.py`
- `backend/tests/integration/api/test_agents.py`
- `frontend/src/features/agents/*`
- `frontend/src/app/router.tsx`
- `frontend/src/app/App.test.tsx`
- `tests/scripts/phase4_smoke.py`
- `tests/scripts/run-phase4-smoke.ps1`
- `scripts/verify.ps1`
- `tests/scripts/run-contract-tests.ps1`
- `README.md`
- `docs/operations/local-development.md`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 10 与阶段 4 验证

- Ruff：全部通过。
- 后端完整测试：`785 passed, 1 skipped`；唯一警告为 Windows 工作树 Pytest 缓存写入，不影响结果。
- 前端：ESLint、TypeScript、`55 passed` 和生产构建全部通过。
- 固定 RAG 安全评测：`16 passed`。
- PowerShell 脚本合同：`50 passed`。
- Alembic：`upgrade head` → `downgrade base` → `upgrade head` 通过。
- 阶段 3/4 离线 smoke 通过；`REAL_CLOUD_PATHS_TESTED=False`，未调用真实 DeepSeek、Embedding、Milvus、Reranker 或安全设备。


## 阶段 5 启动

- 从阶段 4 完整门禁通过的提交创建 `codex/phase-5-trusted-tool-calling` 分支。
- 新增阶段 5 设计规格，明确可信边界、状态机、工具注册、风险策略、审批、幂等、执行租约、受限重试、验证、审计和紧急停止。
- 新增 12 个垂直任务的实施计划；默认离线，不实现阶段 6 ReAct，不接入真实设备，不允许任意 Shell、代码或 URL 工具。
- 路线图将阶段 5 标记为进行中，Task 1 为当前任务。

## 阶段 5 启动修改文件

- `docs/superpowers/specs/2026-07-23-phase-5-trusted-tool-calling-design.md`
- `docs/superpowers/plans/2026-07-23-phase-5-trusted-tool-calling.md`
- `docs/plans/development-roadmap.md`
- `Codex.md`
- `development-logs/2026-07-23.md`

## 阶段 5 下一步

- 实施 Task 1：可信工具调用核心领域合同与白名单状态机。

## 阶段 5 Task 1

- 新增可信工具名称、版本、目标、风险、调用状态、策略结果、审批、执行结果和验证结果枚举。
- 新增不可变 `ToolDefinition`，限制角色、超时、重试、变更工具验证器和高风险盲重试；只读工具不能声明改变状态。
- 新增 `TrustedToolRequest`，服务端绑定案件、运行、计划、角色、版本、证据、预期状态和回滚策略，并生成稳定规范化 SHA-256 请求摘要。
- 结构化参数拒绝 tenant/principal、密钥、凭据、prompt、思维链、Shell、代码、脚本和任意 URL/URI 字段。
- 新增策略与审批有效期、请求摘要绑定、执行尝试和验证合同；成功执行不能带错误，超时可显式表示结果未知。
- 新增白名单只前进状态机；已进入执行的调用不能伪装成紧急停止已经撤回，只能验证、失败或转人工复核。

## Task 1 修改文件

- `backend/src/shieldchain/tools/__init__.py`
- `backend/src/shieldchain/tools/domain.py`
- `backend/tests/unit/tools/__init__.py`
- `backend/tests/unit/tools/test_domain.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 1 验证

- Task 1 聚焦测试：`26 passed`。
- 可信工具领域与阶段 4 编排边界回归：`32 passed`。
- Ruff：`All checks passed`。
- 唯一告警为 Windows 工作树 Pytest 缓存写入，不影响结果。


## 阶段 5 Task 2

- 新增固定 `(tool_name, version)` 注册表；未知工具和未知版本返回相同失败语义，避免枚举注册信息。
- 注册防火墙查询/封禁、终端查询/隔离、账号查询/禁用六个离线工具合同；变更工具必须解析到唯一、只读、同目标类型验证器。
- 新增 IPv4、终端 ID、账号 ID、原因码、TTL 和预期状态的内置严格 Schema；缺失、额外、错误类型和越界字段全部拒绝。
- IPv4 参数规范化并拒绝 loopback、multicast 和 unspecified；终端/账号原因只能使用固定安全枚举。
- 参数和预期状态在绑定后重新冻结，并生成稳定参数摘要与完整请求摘要。
- 注册表启动时拒绝重复身份、Schema/目标类型不匹配以及可变或风险不正确的验证器。

## Task 2 修改文件

- `backend/src/shieldchain/tools/registry.py`
- `backend/tests/unit/tools/test_registry.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 2 验证

- Task 2 聚焦测试：`11 passed`。
- Task 1–2 可信工具领域回归：`37 passed`。
- Ruff：`All checks passed`。
- 唯一告警为 Windows 工作树 Pytest 缓存写入，不影响结果。

## Task 2 风险与下一步

- 当前注册表只定义工具身份和参数合同，不执行工具，也不做策略授权。

## 阶段 5 Task 3

- 新增可信工具调用聚合、策略决策、审批、执行尝试和验证 SQLAlchemy 表；所有子表使用 `(tool_call_id, tenant_id)` 复合外键。
- 调用聚合保存服务端案件/运行/计划绑定、规范化参数、证据、请求摘要、状态和 revision；幂等键在 tenant/run/tool 范围内唯一。
- 新增 Alembic `20260723_01`，支持从阶段 4 head 升级、降回 `20260720_01` 并重新升级。
- 新增 tenant-scoped 仓储：创建前校验同租户 run/case 和已确认事件证据；跨租户统一不可见。
- 同幂等键且同摘要返回已有调用；同键不同摘要显式冲突，不重复执行。
- 状态更新使用 revision/status CAS；stale writer 失败关闭。策略、审批、执行尝试和验证只追加。
- 调用创建和状态转换同步追加现有事件审计，不保存 prompt、思维链、凭据或原始工具输出。

## Task 3 修改文件

- `backend/src/shieldchain/db/base.py`
- `backend/src/shieldchain/tools/persistence.py`
- `backend/src/shieldchain/tools/repositories.py`
- `backend/migrations/versions/20260723_01_add_trusted_tool_gateway.py`
- `backend/tests/unit/tools/test_persistence.py`
- `backend/tests/integration/tools/__init__.py`
- `backend/tests/integration/tools/test_repositories.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 3 验证

- 可信工具领域、持久化和仓储聚焦测试：`41 passed`。
- 阶段 4 代理持久化/API 与阶段 5 回归：`53 passed`。
- Alembic：`upgrade head` → `downgrade 20260720_01` → `upgrade head` 通过。
- Ruff 与 Git diff check 通过；唯一告警为 Windows Pytest 缓存写入，不影响结果。

## Task 3 风险与下一步


## 阶段 5 Task 4

- 新增服务端 `ToolPolicyContext`，防御性冻结 tenant/principal、案件/运行、角色、模式、允许工具、允许目标、已确认证据、预算和速率窗口。
- 策略按自动化开关、紧急停止、工具身份、角色、案件绑定、证据、目标、预算、速率和风险固定顺序检查。
- 所有失败使用稳定原因码并默认拒绝；多个条件同时失败时仍保持确定性优先级。
- 只读/低风险通过安全检查后自动允许；真实模式中高风险需要审批。
- 仿真高风险记录自动策略批准；critical 即使在仿真模式也默认要求审批，只有服务端显式策略开关才能自动批准。
- 风险来自注册表定义，调用参数和模型输出不能覆盖。

## Task 4 修改文件

- `backend/src/shieldchain/tools/policy.py`
- `backend/tests/unit/tools/test_policy.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 4 验证

- 策略引擎聚焦测试：`14 passed`。
- 阶段 5 Task 1–4 回归：`55 passed`。
- Ruff：`All checks passed`。
- 唯一告警为 Windows 工作树 Pytest 缓存写入，不影响结果。

## Task 4 下一步

- 实施 Task 5：审批摘要绑定、有效期、撤销、职责分离和竞态保护。
- 当前仓储只接受已确认的事件证据作为执行依据；知识引用可继续用于规划，但不能单独授权动作。
- 下一步 Task 4：实现固定顺序、默认拒绝的策略引擎和仿真/真实模式风险矩阵。
- 下一步 Task 3：新增租户化持久化迁移、CAS 调用聚合、只追加审批/尝试/验证和幂等冲突仓储。
## Task 1 下一步

- 实施 Task 2：固定版本工具注册表、严格参数解析和规范化请求摘要。
## 风险与遗留

- 工作台当前需要手工输入调查运行 ID；自动从事件调查页跳转属于后续完整前端阶段。
- 轨迹 API 是只读投影，不提供执行入口；真实动作仍必须等待阶段 5 的可信工具网关与审批策略。
- 当前 SQLite/离线替身仅证明本地确定性合同，不代表真实云模型、向量服务或生产数据库已经验收。

## 下一步

- 阶段 4 已完成；下一阶段为阶段 5 可信工具调用，开始前保持当前响应角色仅输出 proposed actions。
