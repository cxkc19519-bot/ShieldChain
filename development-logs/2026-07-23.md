# 2026-07-23 开发日志

## 阶段 4 Task 8

- 新增七类专业角色统一请求、结果和注册表协议；角色输入只接受 Task 5 生成的 `AssembledContext`。
- 告警研判和威胁调查复用阶段 2 确定性钓鱼规则及可信证据适配端口，证据超时和不可用显式传播。
- 知识检索角色直接调用阶段 3 RAG 领域端口，只接受 `GroundedAnswer` 或 `StructuredRefusal`；引用转换为同案件 `KnowledgeReference`。
- 响应规划角色只输出 `proposed:` 动作且不持有工具端口；验证和报告角色在缺少可信事实时拒绝执行。
- 新增可插拔严格模型端口和确定性离线 fake；未知字段、畸形 Schema、超时和依赖失败不会包装成成功。
- 通用模型适配器禁止替换知识检索角色；用于响应规划时会强制把动作降为 `proposed:`。

## 修改文件

- `backend/src/shieldchain/agents/roles.py`
- `backend/tests/unit/agents/test_roles.py`
- `backend/tests/unit/agents/test_roles_security.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## 验证

- Task 8 聚焦测试：`10 passed`。
- 智能体、事件规则和 RAG 相关回归：`285 passed`。
- Ruff：`All checks passed`。
- 测试全程离线，不调用 DeepSeek、Embedding、Milvus 或 Reranker。

## 风险与遗留

- 默认角色实现是确定性离线实现；真实模型和真实 RAG 服务仍需在获得授权后通过现有端口接线。
- 角色只生成分析结果和 proposed actions，尚未实现 Task 9 编排状态机，也没有阶段 5 工具执行权限。
- 当前 Windows 工作树仍有非阻断 Pytest 缓存写入警告；测试结果不受影响。

## 阶段 4 Task 9

- 新增超级智能体白名单状态机，按研判、调查、可选检索、响应规划、可信执行边界、验证和报告的固定路径推进。
- 新增确定性安全监督器，在角色调用前检查步骤与 token 预算，并把拒绝、超时、依赖不可用、证据不足和无效输出映射为稳定原因码。
- 响应规划只进入 `awaiting_trusted_execution`，编排器不持有工具端口；高风险 proposed action 只能等待后续可信执行确认。
- 每一步把角色输出、结构化 handoff、审计事件和预算快照组成单个原子提交包；提交失败保留原状态并允许安全重试。
- 可信执行恢复必须显式传入已验证结果，否则转入人工复核；不存在自由循环或未计费重规划。

## Task 9 修改文件

- `backend/src/shieldchain/agents/orchestrator.py`
- `backend/tests/unit/agents/test_orchestrator.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 9 验证

- 聚焦测试：`6 passed`。
- 智能体单元与集成回归：`144 passed`。
- Ruff：`All checks passed`。
- 测试全程离线；未引入工具执行、云模型或外部检索调用。

## Task 9 风险与遗留

- 原子提交通过端口契约表达，SQLite 持久化与只读协作轨迹将在 Task 10 接线。
- 当前 Windows 工作树仍有非阻断 Pytest 缓存写入警告；测试结果不受影响。

## 阶段 4 Task 10

- 新增 `GET /api/v1/agents/runs/{run_id}/trajectory` 只读 API，使用服务端可信租户边界查询共享上下文、角色执行、交接和可信引用。
- 公开响应只包含共享摘要、已确认事实、角色状态、交接、引用、预算和原因码；查询层从不读取私有上下文表。
- 安全测试验证跨租户返回不存在，且响应不包含 tenant/principal、私有上下文、操作员目标、原始提示或思维链。
- 将 `/agents` 从占位页替换为最小只读工作台，支持按调查运行 ID 查看协作状态、预算、原因码、交接和引用。
- 新增阶段 4 smoke：在临时 SQLite 迁移到 head 后，用离线角色执行确定性编排并原子保存输出、handoff 和审计，再验证公开轨迹及泄漏边界。
- 完整验证脚本升级为阶段 4 门禁，同时保留阶段 3 RAG smoke 前置；脚本合同同步更新。

## Task 10 修改文件

- `backend/src/shieldchain/agents/schemas.py`
- `backend/src/shieldchain/agents/trajectory.py`
- `backend/src/shieldchain/api/agents.py`
- `backend/src/shieldchain/main.py`
- `backend/tests/integration/api/test_agents.py`
- `frontend/src/features/agents/*`
- `frontend/src/app/router.tsx`
- `frontend/src/app/App.test.tsx`
- `tests/scripts/phase4_smoke.py`
- `tests/scripts/run-phase4-smoke.ps1`
- `scripts/verify.ps1`
- `tests/scripts/run-contract-tests.ps1`
- `README.md`
- `docs/operations/local-development.md`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 10 与阶段 4 验证

- Ruff：全部通过。
- 后端完整测试：`785 passed, 1 skipped`；唯一警告为 Windows 工作树 Pytest 缓存写入，不影响结果。
- 前端：ESLint、TypeScript、`55 passed` 和生产构建全部通过。
- 固定 RAG 安全评测：`16 passed`。
- PowerShell 脚本合同：`50 passed`。
- Alembic：`upgrade head` → `downgrade base` → `upgrade head` 通过。
- 阶段 3/4 离线 smoke 通过；`REAL_CLOUD_PATHS_TESTED=False`，未调用真实 DeepSeek、Embedding、Milvus、Reranker 或安全设备。


## 阶段 5 启动

- 从阶段 4 完整门禁通过的提交创建 `codex/phase-5-trusted-tool-calling` 分支。
- 新增阶段 5 设计规格，明确可信边界、状态机、工具注册、风险策略、审批、幂等、执行租约、受限重试、验证、审计和紧急停止。
- 新增 12 个垂直任务的实施计划；默认离线，不实现阶段 6 ReAct，不接入真实设备，不允许任意 Shell、代码或 URL 工具。
- 路线图将阶段 5 标记为进行中，Task 1 为当前任务。

## 阶段 5 启动修改文件

- `docs/superpowers/specs/2026-07-23-phase-5-trusted-tool-calling-design.md`
- `docs/superpowers/plans/2026-07-23-phase-5-trusted-tool-calling.md`
- `docs/plans/development-roadmap.md`
- `Codex.md`
- `development-logs/2026-07-23.md`

## 阶段 5 下一步

- 实施 Task 1：可信工具调用核心领域合同与白名单状态机。

## 阶段 5 Task 1

- 新增可信工具名称、版本、目标、风险、调用状态、策略结果、审批、执行结果和验证结果枚举。
- 新增不可变 `ToolDefinition`，限制角色、超时、重试、变更工具验证器和高风险盲重试；只读工具不能声明改变状态。
- 新增 `TrustedToolRequest`，服务端绑定案件、运行、计划、角色、版本、证据、预期状态和回滚策略，并生成稳定规范化 SHA-256 请求摘要。
- 结构化参数拒绝 tenant/principal、密钥、凭据、prompt、思维链、Shell、代码、脚本和任意 URL/URI 字段。
- 新增策略与审批有效期、请求摘要绑定、执行尝试和验证合同；成功执行不能带错误，超时可显式表示结果未知。
- 新增白名单只前进状态机；已进入执行的调用不能伪装成紧急停止已经撤回，只能验证、失败或转人工复核。

## Task 1 修改文件

- `backend/src/shieldchain/tools/__init__.py`
- `backend/src/shieldchain/tools/domain.py`
- `backend/tests/unit/tools/__init__.py`
- `backend/tests/unit/tools/test_domain.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 1 验证

- Task 1 聚焦测试：`26 passed`。
- 可信工具领域与阶段 4 编排边界回归：`32 passed`。
- Ruff：`All checks passed`。
- 唯一告警为 Windows 工作树 Pytest 缓存写入，不影响结果。


## 阶段 5 Task 2

- 新增固定 `(tool_name, version)` 注册表；未知工具和未知版本返回相同失败语义，避免枚举注册信息。
- 注册防火墙查询/封禁、终端查询/隔离、账号查询/禁用六个离线工具合同；变更工具必须解析到唯一、只读、同目标类型验证器。
- 新增 IPv4、终端 ID、账号 ID、原因码、TTL 和预期状态的内置严格 Schema；缺失、额外、错误类型和越界字段全部拒绝。
- IPv4 参数规范化并拒绝 loopback、multicast 和 unspecified；终端/账号原因只能使用固定安全枚举。
- 参数和预期状态在绑定后重新冻结，并生成稳定参数摘要与完整请求摘要。
- 注册表启动时拒绝重复身份、Schema/目标类型不匹配以及可变或风险不正确的验证器。

## Task 2 修改文件

- `backend/src/shieldchain/tools/registry.py`
- `backend/tests/unit/tools/test_registry.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 2 验证

- Task 2 聚焦测试：`11 passed`。
- Task 1–2 可信工具领域回归：`37 passed`。
- Ruff：`All checks passed`。
- 唯一告警为 Windows 工作树 Pytest 缓存写入，不影响结果。

## Task 2 风险与下一步

- 当前注册表只定义工具身份和参数合同，不执行工具，也不做策略授权。

## 阶段 5 Task 3

- 新增可信工具调用聚合、策略决策、审批、执行尝试和验证 SQLAlchemy 表；所有子表使用 `(tool_call_id, tenant_id)` 复合外键。
- 调用聚合保存服务端案件/运行/计划绑定、规范化参数、证据、请求摘要、状态和 revision；幂等键在 tenant/run/tool 范围内唯一。
- 新增 Alembic `20260723_01`，支持从阶段 4 head 升级、降回 `20260720_01` 并重新升级。
- 新增 tenant-scoped 仓储：创建前校验同租户 run/case 和已确认事件证据；跨租户统一不可见。
- 同幂等键且同摘要返回已有调用；同键不同摘要显式冲突，不重复执行。
- 状态更新使用 revision/status CAS；stale writer 失败关闭。策略、审批、执行尝试和验证只追加。
- 调用创建和状态转换同步追加现有事件审计，不保存 prompt、思维链、凭据或原始工具输出。

## Task 3 修改文件

- `backend/src/shieldchain/db/base.py`
- `backend/src/shieldchain/tools/persistence.py`
- `backend/src/shieldchain/tools/repositories.py`
- `backend/migrations/versions/20260723_01_add_trusted_tool_gateway.py`
- `backend/tests/unit/tools/test_persistence.py`
- `backend/tests/integration/tools/__init__.py`
- `backend/tests/integration/tools/test_repositories.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 3 验证

- 可信工具领域、持久化和仓储聚焦测试：`41 passed`。
- 阶段 4 代理持久化/API 与阶段 5 回归：`53 passed`。
- Alembic：`upgrade head` → `downgrade 20260720_01` → `upgrade head` 通过。
- Ruff 与 Git diff check 通过；唯一告警为 Windows Pytest 缓存写入，不影响结果。

## 阶段 5 Task 11

- 新增 tenant-bound 可信工具轨迹 API，按调查运行返回工具、目标、状态、策略、审批、执行尝试、验证、稳定原因码和证据 ID。
- 轨迹投影不返回租户、主体、原始工具结果、令牌摘要、模型提示或私有推理；所有策略、审批、尝试和验证子查询再次显式绑定租户。
- 新增批准/拒绝、暂停/恢复/取消和租户级紧急停止 API；租户和操作主体只从服务端状态绑定，请求 Schema 拒绝额外授权字段。
- 审批和控制领域冲突映射为安全的 `409`，不存在或跨租户资源映射为 `404`，不泄漏内部异常。
- 新增最小处置中心，支持按运行 ID 查看公开轨迹、填写操作原因、执行审批/控制和紧急停止；页面明确不展示原始结果、凭据或私有推理。
- 前端响应解析保持显式公开契约，变更请求只发送 outcome、reason 或 active，不发送 tenant/principal/permission。

## Task 11 修改文件

- `backend/src/shieldchain/main.py`
- `backend/src/shieldchain/api/tools.py`
- `backend/src/shieldchain/tools/api_service.py`
- `backend/src/shieldchain/tools/schemas.py`
- `backend/tests/integration/api/test_trusted_tools_api.py`
- `frontend/src/app/router.tsx`
- `frontend/src/app/App.test.tsx`
- `frontend/src/features/tools/api.ts`
- `frontend/src/features/tools/types.ts`
- `frontend/src/features/tools/ToolsPage.tsx`
- `frontend/src/features/tools/tools.css`
- `frontend/src/features/tools/api.test.ts`
- `frontend/src/features/tools/ToolsPage.test.tsx`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 11 验证

- 后端 API 与可信工具聚焦回归：`100 passed`。
- 前端处置中心和应用路由：`13 passed`；TypeScript 类型检查与 ESLint 通过。
- Ruff 通过；唯一告警为 Windows Pytest 缓存写入，不影响结果。

## Task 11 下一步

- 实施 Task 12：完整离线可信工具 smoke、迁移往返、安全评测、文档和阶段 5 出口门禁。

## 阶段 5 Task 12

- 新增阶段 5 离线 smoke，在独立临时 SQLite 上迁移到 head 后执行真实生产领域/仓储路径。
- 成功场景覆盖 proposed action 绑定、确定性策略、显式仿真审批、执行租约、离线适配器、执行后只读验证、`succeeded + verified` 编排恢复。
- 失败关闭场景覆盖人工审批拒绝、同幂等键不同摘要冲突、变更工具超时结果未知并进入人工复核、紧急停止待执行调用。
- smoke 校验公开轨迹不泄漏 tenant/principal、租约 token digest、原始结果摘要、提示或私有推理；不联网、不执行 Shell、不访问真实设备。
- `verify.ps1` 最终门禁切换为阶段 5 smoke，并把临时目录命名/清理边界升级为 phase 5；PowerShell 合同同步校验新的门禁顺序和失败传递。
- 新增 smoke 静态合同测试，确保前置阶段、临时清理、离线标记、核心场景和泄漏字段不会被后续修改绕过。
- 更新 README、可信工具架构、本地开发说明和路线图；明确单机 SQLite/离线仿真限制及真实设备路径未测试。
- 完整测试发现并修复新增 API 测试与既有事件工具测试的 Python 模块同名冲突；测试文件改用唯一名称。

## Task 12 修改文件

- `tests/scripts/phase5_smoke.py`
- `tests/scripts/run-phase5-smoke.ps1`
- `backend/tests/integration/tools/test_phase5_smoke_contract.py`
- `backend/tests/integration/api/test_trusted_tools_api.py`
- `scripts/verify.ps1`
- `tests/scripts/run-contract-tests.ps1`
- `README.md`
- `docs/architecture/trusted-tool-calling.md`
- `docs/operations/local-development.md`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 12 完整验证

- Ruff：通过。
- 后端完整 Pytest：`897 passed, 1 skipped`；跳过项为未授权付费 DeepSeek 实时测试。
- 前端 ESLint、TypeScript 与生产构建：通过；Vitest `58 passed`。
- Alembic 临时 SQLite：`upgrade head` → `downgrade base` → `upgrade head`，最终 `20260723_03 (head)`。
- 固定双语 RAG/安全评测：`16 passed`。
- PowerShell 脚本合同：`51 passed`。
- 阶段 3、阶段 4、阶段 5 离线 smoke：全部通过；输出 `REAL_CLOUD_PATHS_TESTED=False` 与 `REAL_DEVICE_PATHS_TESTED=False`。
- 唯一非阻断告警为 Windows Pytest 缓存目录创建告警，不影响测试和产物。

## 阶段 5 结论

- 阶段 5 的 12 个 Task 已全部完成并满足退出条件。
- 未推送、未合并；真实防火墙、EDR、IAM 和云模型/检索服务仍未授权或验收，不宣称真实环境成功。
- 后续开发入口为阶段 6 ReAct 自主闭环设计与实施。


## 阶段 6 启动

- 从阶段 5 完成提交创建 `codex/phase-6-react-loop` 分支。
- 新增阶段 6 受控 ReAct 设计规格和 12 个小步实施 Task。
- 明确 ReAct 只保存结构化观察、固定失败分类、计划差异、决策、预算和引用，不保存或展示自由思维链。
- 重新规划器为确定性策略；任何 proposed action 仍须重新经过阶段 5 工具注册、策略、审批、幂等、执行和验证。
- 变更动作结果未知时只允许配对只读查询，审批拒绝、紧急停止、证据冲突、循环和预算耗尽转人工。
- 阶段 6 默认测试继续离线，不联网、不产生费用、不执行 Shell、不接真实设备；阶段 7 完整前端保持未开始。

## 阶段 6 启动修改文件

- `docs/superpowers/specs/2026-07-23-phase-6-react-loop-design.md`
- `docs/superpowers/plans/2026-07-23-phase-6-react-loop.md`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`
- `Codex.md`

下一步：实施 Task 1 结构化 ReAct 领域合同。

## 阶段 6 Task 1

- 新增 `react` 包和受控 ReAct 纯领域合同。
- 固定循环状态、观察来源、13 类失败分类和 6 类恢复/继续决策；未知类别明确为 `unclassified_failure`。
- 观察只保存结构化 status/reason code、可信引用及可选工具/验证 ID；工具观察必须绑定真实调用，验证观察必须同时绑定验证 ID。
- proposed action 仅允许封禁 IP、隔离终端和禁用账号三类白名单动作，必须携带目标、预期状态和可信引用。
- 计划修订强制线性 parent revision、保留/删除集合互斥、新增动作同案件引用和只追加历史语义。
- 循环绑定 case/run、revision、阶段 4 `BudgetSnapshot`、观察 SHA-256 指纹与 UTC 时间；决策快照绑定观察、分类和预算。
- 所有集合/映射防御性冻结，所有公开 `to_dict` 为 JSON-safe，且不包含租户主体、原始提示、思维链、结果摘要、租约令牌或密钥。

## Task 1 修改文件

- `backend/src/shieldchain/react/__init__.py`
- `backend/src/shieldchain/react/domain.py`
- `backend/tests/unit/react/test_domain.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 1 验证

- ReAct 领域聚焦测试：`17 passed`。
- Ruff format/check：通过。
- 唯一告警为 Windows Pytest 缓存目录创建，不影响结果。

## Task 1 下一步

- 实施 Task 2：从受信角色、调用、尝试、验证和控制终态生成固定失败分类与允许决策。

## 阶段 6 Task 2

- 新增确定性失败分类器，只接受阶段 4 `RoleExecutionResult`、阶段 5 `TrustedToolCall`/attempt/verification 及服务端控制条件。
- 分类器不解析或复制 error/result summary 等自由文本，只依据固定枚举、稳定 reason 和严格对象绑定。
- 固定优先级为循环检测、预算、紧急停止、自动化关闭、证据冲突、验证、执行尝试、调用 reason、角色终态；未知组合默认 `unclassified_failure`。
- 工具调用、执行尝试和验证必须绑定观察的同 case/run/call/verification ID；跨边界对象立即拒绝。
- `execution_outcome_unknown` 只允许 `query_status` 或 `manual_review`，禁止重放变更或直接重新规划。
- 审批拒绝、紧急停止、自动化关闭、证据冲突、预算耗尽、循环和不可分类失败只允许人工复核。
- 分类 ID 使用 observation/category 的 UUID5，可安全幂等重建；所有失败类别都有显式非空决策集合。

## Task 2 修改文件

- `backend/src/shieldchain/react/classification.py`
- `backend/tests/unit/react/test_classification.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 2 验证

- 阶段 6 领域与分类器回归：`36 passed`。
- Ruff format/check 与 Git diff check：通过。
- 唯一告警为 Windows Pytest 缓存目录创建，不影响结果。

## Task 2 下一步

- 实施 Task 3：服务端预算预计消费、时间/费用/工具计数和规范化观察/计划指纹循环检测。

## 阶段 6 Task 3

- 新增服务端 `ReactConsumption`，只接受有硬上界的非负 Token、费用和工具调用计数。
- 预算监督器在新 step 前投影 steps、loops、墙钟时间、tokens、cost 和 tool calls 六个维度；当前已到上限或预计超限均不消费预算并返回 `budget_exhausted`。
- 墙钟时间从可信 loop `started_at` 和服务端 `now` 计算，禁止时钟倒退，不接受客户端累计时间。
- 观察与计划必须绑定同 loop/case/run；跨边界输入在预算计算前拒绝。
- 循环指纹只包含规范化来源、状态、原因、绑定调用/验证、排序引用和计划差异，不包含观察 ID 或时间戳。
- 同一语义观察与计划再次出现时，在预算消费前返回 `loop_detected`；证据或计划变化会产生不同指纹。
- 预算投影返回不可变 `BudgetSnapshot`，不修改原循环状态，为 Task 6 原子 CAS 提交准备。

## Task 3 修改文件

- `backend/src/shieldchain/react/budget.py`
- `backend/tests/unit/react/test_budget.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 3 验证

- 阶段 6 领域、分类器、预算和循环检测回归：`52 passed`。
- Ruff format/check 与 Git diff check：通过。
- 首轮测试正确发现重复观察夹具使用了不同证据 ID；修复夹具后通过。
- 唯一告警为 Windows Pytest 缓存目录创建，不影响结果。

## Task 3 下一步


## 阶段 6 Task 4

- 新增确定性计划修订器，输出严格区分 `query_status`、`retry_read_only`、`replan` 和 `manual_review`。
- 变更工具结果未知或验证不确定时，从注册表读取该工具固定 verifier，只生成同目标只读查询，不创建或重放变更动作。
- 只读依赖失败仅返回同一注册只读工具重试指令；实际重试上限仍由阶段 5 注册与 Task 3 预算约束。
- 审批拒绝、紧急停止、自动化关闭、证据冲突、预算耗尽、循环和不可分类失败不生成计划，直接人工复核。
- 明确执行/验证失败和证据不足只有在存在服务端提供、注册表确认、同案件可信引用的替代候选时才重规划。
- 相同动作+目标不会被当作替代方案；候选按 action/target/ID 稳定排序，输入顺序不影响选择。
- 新计划使用 loop/revision 的确定性 UUID5，parent revision 线性递增，失败动作进入 removed，其他动作保留，候选只以 `proposed:*` 进入。

## Task 4 修改文件

- `backend/src/shieldchain/react/replanning.py`
- `backend/tests/unit/react/test_replanning.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 4 验证

- 阶段 6 聚焦回归：`65 passed`。
- ReAct、阶段 4 智能体和阶段 5 可信工具跨模块回归：`298 passed`。
- Ruff format/check 与 Git diff check：通过。
- 唯一告警为 Windows Pytest 缓存目录创建，不影响结果。

## Task 4 下一步

- 实施 Task 5：循环运行、观察、分类、计划修订和决策持久化模型及可逆 Alembic 迁移。

## 阶段 6 Task 5

- 新增循环、观察、分类、计划修订和决策五张 SQLAlchemy 表。
- 循环表通过 `(run_id, tenant_id)` 组合外键绑定调查运行，并保存 revision、完整预算、观察指纹和 UTC 时间。
- 观察、分类、计划、决策使用 `(parent_id, tenant_id)` 组合外键；观察和分类额外提供 `(id, tenant_id)` 唯一键供后续轨迹绑定。
- 状态、来源、分类、决策、confidence、iteration 和 revision 在数据库层使用 CheckConstraint；同 loop 计划 revision 唯一。
- 新增可逆 Alembic `20260723_04`，按依赖顺序建表/索引并反向删除。

## Task 5 修改文件

- `backend/src/shieldchain/react/persistence.py`
- `backend/src/shieldchain/db/base.py`
- `backend/migrations/versions/20260723_04_add_react_loops.py`
- `backend/tests/unit/react/test_persistence.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 5 验证

- ReAct metadata/约束测试：`1 passed`。
- Alembic 临时 SQLite：`upgrade head` → `downgrade 20260723_03` → `upgrade head`，最终 `20260723_04 (head)`。
- Ruff format/check 与 Git diff check：通过。

## Task 5 下一步


## 阶段 6 Task 6

- 新增 tenant-bound ReAct 仓储，创建前验证服务端调查运行和案件绑定，读取在 SQL 层同时约束 tenant 与 loop ID。
- 循环更新使用 `tenant_id + id + expected revision` CAS；陈旧 revision 返回稳定 `StaleReactLoop`。
- `ReactStepBundle` 原子提交 changed loop、观察、分类、可选计划修订和决策；预算与观察指纹随 loop CAS 一起更新。
- 提交前校验所有记录绑定同 loop/case/run，assessment/decision 绑定同 observation，计划与 replan decision 一一对应。
- CAS 失败发生在任何 append row 加入前；回滚后不会留下半步观察、分类或决策。

## Task 6 修改文件

- `backend/src/shieldchain/react/repositories.py`
- `backend/tests/integration/react/test_repositories.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 6 验证

- ReAct 单元/集成聚焦回归：`68 passed`。
- ReAct、智能体、可信工具跨模块回归：`310 passed`。
- Ruff format/check 与 Git diff check：通过。
- 唯一告警为 Windows Pytest 缓存目录创建，不影响结果。

## Task 6 下一步

## 阶段 6 Task 7

- 新增一步式 `ControlledReactService`，组合预算投影、固定分类、确定性修订和原子仓储。
- 服务不接收或持有工具适配器、凭据、模型提示、任意命令/URL；只接收受信领域对象与固定工具注册表。
- 预算或循环停止时不消费预算、不生成计划，原子提交人工复核决策。
- 允许继续时只输出注册表配对查询、只读重试、白名单 proposed plan 或人工复核。
- 每步决策使用 loop/revision UUID5；loop revision、预算、指纹、观察、分类、计划和决策通过一个 bundle 提交。
- 新增 SQLAlchemy step store 适配器，使用 savepoint 调用 tenant-bound CAS 仓储。

## Task 7 修改文件

- `backend/src/shieldchain/react/service.py`
- `backend/tests/unit/react/test_service.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 7 验证

- 阶段 6 ReAct 单元/集成回归：`70 passed`。
- Ruff format/check 与 Git diff check：通过。
- 唯一告警为 Windows Pytest 缓存目录创建，不影响结果。

## Task 7 下一步

## 阶段 6 Task 8

- 新增过期 `running` loop 扫描，SQL 同时约束 tenant、status 和服务端截止时间，并按更新时间/ID 稳定排序。
- 恢复 worker 使用 tenant+status+revision CAS 抢占；多个 worker 使用同一陈旧对象时最多一个 revision 更新成功。
- 恢复决策器复用阶段 5 `RecoveryDecision`，映射状态查询、只读安全重试、验证、等待审批和人工复核。
- 变更动作未知的 `QUERY_STATUS` 保持为 `query_tool_status`，不会被转换成 resume/replan 或动作重放。
- 非 running loop 无论外部恢复建议为何均转人工；没有未完成工具工作时才允许恢复中断的纯 ReAct step。

## Task 8 修改文件

- `backend/src/shieldchain/react/recovery.py`
- `backend/src/shieldchain/react/repositories.py`
- `backend/tests/unit/react/test_recovery.py`
- `backend/tests/integration/react/test_repositories.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 8 验证

- 阶段 6 ReAct 单元/集成回归：`74 passed`。
- Ruff format/check 与 Git diff check：通过。

## Task 8 下一步

- 实施 Task 9：把阶段 5 未验证终态交给受控循环，verified 成功仍直接继续阶段 4 验证角色。

- 实施 Task 8：过期 running loop 恢复扫描、阶段 5 租约恢复委托和多 worker 唯一提交。

- 实施 Task 7：组合观察、分类、预算和修订的一步式受控循环服务，不持有工具适配器。
- 实施 Task 6：tenant-bound 循环 CAS、只追加轨迹和 observation+assessment+plan+decision+budget 原子提交。
- 实施 Task 4：根据固定失败类别、当前计划和注册表配对能力生成确定性安全计划差异。
## Task 3 风险与下一步


## 阶段 5 Task 4

- 新增服务端 `ToolPolicyContext`，防御性冻结 tenant/principal、案件/运行、角色、模式、允许工具、允许目标、已确认证据、预算和速率窗口。
- 策略按自动化开关、紧急停止、工具身份、角色、案件绑定、证据、目标、预算、速率和风险固定顺序检查。
- 所有失败使用稳定原因码并默认拒绝；多个条件同时失败时仍保持确定性优先级。
- 只读/低风险通过安全检查后自动允许；真实模式中高风险需要审批。
- 仿真高风险记录自动策略批准；critical 即使在仿真模式也默认要求审批，只有服务端显式策略开关才能自动批准。
- 风险来自注册表定义，调用参数和模型输出不能覆盖。

## Task 4 修改文件

- `backend/src/shieldchain/tools/policy.py`
- `backend/tests/unit/tools/test_policy.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 4 验证

- 策略引擎聚焦测试：`14 passed`。
- 阶段 5 Task 1–4 回归：`55 passed`。
- Ruff：`All checks passed`。
- 唯一告警为 Windows 工作树 Pytest 缓存写入，不影响结果。

## Task 4 下一步

## 阶段 5 Task 5

- 新增审批服务，审批必须绑定当前调用 ID、规范化请求摘要、策略版本和未过期策略决策。
- 高风险审批要求 `trusted_tools.approve`，critical 要求独立的 `trusted_tools.approve_critical` 权限。
- 高风险和 critical 调用实行申请者/审批者职责分离，申请主体不能批准自己的调用。
- 同摘要同结果的重复审批幂等返回已有决定；冲突结果或参数变化显式拒绝，不覆盖历史审批。
- 审批拒绝、批准和自动仿真策略都形成不可变 `ApprovalDecision`；自动批准使用固定服务主体并明确记录来源。
- 新增 tenant-scoped SQLAlchemy 审批存储适配器，只读取当前租户最新审批并通过只追加仓储保存。

## Task 5 修改文件

- `backend/src/shieldchain/tools/approvals.py`
- `backend/src/shieldchain/tools/approval_store.py`
- `backend/tests/unit/tools/test_approvals.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 5 验证

- 审批服务聚焦测试：`6 passed`。
- 审批与仓储集成回归：`9 passed`。
- 阶段 5 Task 1–5 回归：`61 passed`。
- Ruff 和 Git diff check 通过；唯一告警为 Windows Pytest 缓存写入，不影响结果。

## Task 5 下一步

- 实施 Task 6：提议→策略→审批→执行租约→适配器→验证的固定可信工具网关。

- 实施 Task 5：审批摘要绑定、有效期、撤销、职责分离和竞态保护。
- 当前仓储只接受已确认的事件证据作为执行依据；知识引用可继续用于规划，但不能单独授权动作。
- 下一步 Task 4：实现固定顺序、默认拒绝的策略引擎和仿真/真实模式风险矩阵。
- 下一步 Task 3：新增租户化持久化迁移、CAS 调用聚合、只追加审批/尝试/验证和幂等冲突仓储。
## Task 1 下一步

- 实施 Task 2：固定版本工具注册表、严格参数解析和规范化请求摘要。
## 风险与遗留

- 工作台当前需要手工输入调查运行 ID；自动从事件调查页跳转属于后续完整前端阶段。
- 轨迹 API 是只读投影，不提供执行入口；真实动作仍必须等待阶段 5 的可信工具网关与审批策略。
- 当前 SQLite/离线替身仅证明本地确定性合同，不代表真实云模型、向量服务或生产数据库已经验收。

## 下一步

- 阶段 4 已完成；下一阶段为阶段 5 可信工具调用，开始前保持当前响应角色仅输出 proposed actions。

## 阶段 5 Task 6

- 新增固定网关流水线：创建调用→确定性策略→显式审批→执行→执行后验证；适配器只接收 `BoundToolRequest`，不接收模型上下文、主体权限或凭据。
- 策略拒绝和等待人工审批分支不会触达适配器；人工恢复必须匹配租户、案件、运行、调用 ID、请求摘要、策略版本、最新审批及有效期。
- 自动仿真审批、策略决定、状态转换、执行尝试和验证结果均写入既有只追加仓储与审计链路。
- SQLAlchemy 网关存储使用显式 savepoint，把状态转换、尝试/验证记录和对应审计作为同一原子段提交；外部适配器调用不持有数据库事务。
- 适配器异常不会保存私有异常文本；结果摘要去除控制字符并限制长度，错误类别限制为安全标识符。
- 超时严格映射为 `unknown` 和 `needs_review`，不进入验证，也不宣称动作成功；只有执行成功且验证为 `verified` 才进入 `succeeded`。

## Task 6 修改文件

- `backend/src/shieldchain/tools/gateway.py`
- `backend/src/shieldchain/tools/gateway_store.py`
- `backend/tests/unit/tools/test_gateway.py`
- `backend/tests/integration/tools/test_repositories.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 6 验证

- 固定网关聚焦测试：`11 passed`。
- 阶段 5 工具模块跨单元/集成回归：`73 passed`。
- 原子回滚测试确认失败事务不残留执行尝试，调用状态保持执行前已提交状态。
- Ruff 和 Git diff check 通过；唯一告警为 Windows Pytest 缓存写入，不影响结果。

## Task 6 下一步

- 实施 Task 7：同键结果复用、执行租约、超时、注册表限定重试、未知结果状态查询、预算计数与恢复分支。

## 阶段 5 Task 7

- 新增不可变执行租约合同，租约绑定调用、持有主体、尝试序号、短期有效期和随机令牌摘要；数据库只保存 SHA-256，不保存明文令牌。
- 新增 tenant-scoped 执行租约表和 SQLAlchemy 存储；发放前校验调用处于已批准状态及当前 revision，并拒绝同一调用的并发活动租约。
- 网关在 `approved→executing` 的同一事务段中发放租约，在最终执行尝试、租约释放、状态转换和审计的同一事务段中提交结果。
- 只读工具仅对明确 `failed` 按注册表 `max_retries` 有限重试；超时继续表示 `unknown` 且不重试，高风险/critical 变更失败不盲目重放。
- 每次执行尝试和重试追加稳定审计事件；同键同摘要继续复用已有调用，同键不同摘要继续由仓储拒绝。
- 新增服务端 tenant/run 使用量计数、过期活动租约扫描和确定性恢复决策；高风险过期租约先查询状态，只读租约仅在自动化开启、预算充足且未耗尽重试时允许恢复。
- 等待审批、待验证、预算耗尽、自动化关闭和不可恢复状态均进入明确等待或人工复核分支。

## Task 7 修改文件

- `backend/src/shieldchain/tools/execution.py`
- `backend/src/shieldchain/tools/execution_store.py`
- `backend/src/shieldchain/tools/gateway.py`
- `backend/src/shieldchain/tools/gateway_store.py`
- `backend/src/shieldchain/tools/persistence.py`
- `backend/src/shieldchain/tools/repositories.py`
- `backend/migrations/versions/20260723_02_add_tool_execution_leases.py`
- `backend/tests/unit/tools/test_execution.py`
- `backend/tests/unit/tools/test_gateway.py`
- `backend/tests/unit/tools/test_persistence.py`
- `backend/tests/integration/tools/test_repositories.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 7 验证

- 阶段 5 工具模块回归：`87 passed`。
- 阶段 4/5 智能体、事件和可信工具跨模块回归：`440 passed`。
- Alembic 临时 SQLite：`upgrade head` → `downgrade 20260723_01` → `upgrade head`，最终 `20260723_02 (head)`。
- Ruff 与 Git diff check 通过；唯一告警为 Windows Pytest 缓存写入，不影响结果。

## Task 7 下一步

- 实施 Task 8：离线防火墙、终端和账户仿真适配器及配套只读执行后验证。

## 阶段 5 Task 8

- 新增固定 `OfflineSimulationAdapter`，覆盖防火墙查询/封禁、终端查询/隔离和账户查询/禁用六个注册工具。
- 防火墙变更复用现有 `SimulatedFirewall` 和钓鱼场景状态；终端与账户使用固定内存状态，全部目标在构造时显式 allowlist。
- 适配器不访问网络、Shell、系统命令或真实设备，不接收模型上下文、租户权限或凭据。
- 每个变更工具都有目标类型匹配的只读查询路径；执行后验证重新读取状态并与不可变 `expected_state` 比较。
- 只有观测状态完全匹配才返回 `verified`；不匹配返回 `failed` 和稳定 `verification_failed` 原因。
- 未知目标不会动态创建；固定 fail-once 路径返回安全错误类别，适配器摘要不包含目标或私有输入。
- 新增 SQLAlchemy 网关闭环测试，验证策略→自动审批→租约→仿真变更→只读验证→`succeeded` 的完整持久化路径。

## Task 8 修改文件

- `backend/src/shieldchain/tools/simulation.py`
- `backend/tests/unit/tools/test_simulation.py`
- `backend/tests/integration/tools/test_repositories.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 8 验证

- 离线仿真适配器聚焦测试：`11 passed`。
- 仿真适配器与 SQLAlchemy 网关集成回归：`18 passed`。
- 阶段 5 可信工具模块回归：`99 passed`。
- Ruff 与 Git diff check 通过；唯一告警为 Windows Pytest 缓存写入，不影响结果。

## Task 8 下一步

- 实施 Task 9：单调用暂停/取消/恢复、全局自动化开关、紧急停止和并发恢复控制。

## 阶段 5 Task 9

- 新增 `paused` 调用状态；仅尚未发给适配器的 proposed/policy_checked/awaiting_approval/approved 调用可暂停并恢复到审计记录的可信前态。
- 取消同样只允许预执行或已暂停调用；executing/verifying 调用拒绝伪装成 cancelled，必须查询状态并完成验证或人工复核。
- 新增租户级 `AutomationControl`，保存自动化开关、紧急停止、revision、操作主体、原因和时间；SQLAlchemy 更新使用 revision/CAS。
- 新增只追加控制事件表，记录暂停、恢复、取消、开关和紧急停止的主体、原因、前后状态、revision 与时间。
- 紧急停止只把尚未进入适配器的调用转为 `emergency_stopped`；已经 executing 的调用保持执行中，不宣称撤回。
- 执行租约发放新增服务端硬检查：自动化关闭或紧急停止激活时，即使调用方上下文声称允许，也不会发放新租约。
- 所有控制查询按 tenant 约束；恢复依赖最新可信暂停事件，缺少历史事件时默认拒绝。

## Task 9 修改文件

- `backend/src/shieldchain/tools/control.py`
- `backend/src/shieldchain/tools/control_store.py`
- `backend/src/shieldchain/tools/domain.py`
- `backend/src/shieldchain/tools/execution_store.py`
- `backend/src/shieldchain/tools/persistence.py`
- `backend/migrations/versions/20260723_03_add_tool_controls.py`
- `backend/tests/unit/tools/test_control.py`
- `backend/tests/unit/tools/test_persistence.py`
- `backend/tests/integration/tools/test_repositories.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 9 验证

- 控制状态机、持久化和集成聚焦回归：`42 passed`。
- 阶段 5 可信工具模块回归：`107 passed`。
- Alembic 临时 SQLite：`upgrade head` → `downgrade 20260723_02` → `upgrade head`，最终 `20260723_03 (head)`。

## 阶段 6 Task 9

- 编排器恢复入口新增强类型 `ReactDecision`，不接受布尔值或自由文本绕过可信执行边界。
- 已验证成功的可信调用继续进入验证/报告链路，恢复信号不能改变成功终态。
- `query_status`、`retry_read_only` 和 `replan` 在未验证时保持 `awaiting_trusted_execution`，等待受控循环产生下一项可信动作。
- 其余决策与无决策的未验证终态统一进入人工复核；响应角色仍不接触工具适配器。

## Task 9 修改文件

- `backend/src/shieldchain/agents/orchestrator.py`
- `backend/tests/unit/agents/test_orchestrator.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 9 验证

- 编排器、ReAct 与可信工具定向回归：`181 passed`。
- 后端完整单元/集成回归使用 `--import-mode=importlib` 运行，避免不同测试目录同名模块的默认导入冲突。
- Ruff 与 Git diff check 通过；Windows Pytest 缓存告警不影响结果。

## Task 9 下一步

- 实施 Task 10：tenant-bound 只读循环轨迹，以及服务端身份约束的人工接管/恢复 API。
- Ruff 与 Git diff check 通过；唯一告警为 Windows Pytest 缓存写入，不影响结果。

## Task 9 下一步

- 实施 Task 10：把阶段 4 `awaiting_trusted_execution` 与可信工具验证终态连接，失败/未知/取消/紧急停止转人工复核。

## 阶段 5 Task 10

- 移除编排器 `trusted_execution_verified: bool` 恢复入口，防止上层用未绑定布尔值绕过可信工具状态机。
- `resume_after_execution` 现在必须接收实际 `TrustedToolCall` 和对应 `ToolVerification`，并校验案件、调用 ID、终态及验证结果。
- 仅 `call.status == succeeded` 且 `verification.outcome == verified`、验证绑定同一调用时进入阶段 4 验证角色。
- failed、needs_review、rejected、cancelled、emergency_stopped、缺失或不匹配验证统一进入 `needs_review`，使用稳定原因 `trusted_execution_not_verified`。
- 响应规划角色边界保持不变：只输出 `proposed:*` 动作，不获得适配器、租约、审批或真实工具入口。

## Task 10 修改文件

- `backend/src/shieldchain/agents/orchestrator.py`
- `backend/tests/unit/agents/test_orchestrator.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 10 验证

- 编排器聚焦测试：`7 passed`。
- 阶段 4/5 智能体与可信工具跨模块回归：`252 passed`。
- Ruff 与 Git diff check 通过；唯一告警为 Windows Pytest 缓存写入，不影响结果。

## Task 10 下一步

- 实施 Task 11：tenant-bound 调用轨迹、审批/取消/恢复/紧急停止 API，以及不展示私有数据的最小处置中心。

## 阶段 6 Task 10

- 新增 `/api/v1/react/runs/{run_id}/trajectory`，按服务端租户投影循环状态、观察、分类、计划差异、决策、预算、引用及人工控制事件。
- 新增 `/api/v1/react/loops/{loop_id}/takeover|resume`；tenant、actor 和 request ID 全部来自服务端边界，HTTP 不能注入权限、预算、分类或风险。
- 接管/恢复使用 loop revision/CAS；只允许 running/awaiting_execution 接管，恢复严格返回接管前的可信状态，终态和非法转换失败关闭。
- 新增只追加人工控制审计表，保存操作主体、稳定 reason code、原因摘要、request ID、前后状态和 revision；公开轨迹只展示安全白名单字段。
- 引用和 proposed action 采用白名单投影；原始适配器结果、提示、思维链、租户、操作主体、原因摘要和 request ID 不对外返回。

## Task 10 修改文件

- `backend/src/shieldchain/react/schemas.py`
- `backend/src/shieldchain/react/api_service.py`
- `backend/src/shieldchain/api/react.py`
- `backend/src/shieldchain/react/persistence.py`
- `backend/src/shieldchain/react/repositories.py`
- `backend/src/shieldchain/main.py`
- `backend/migrations/versions/20260723_05_add_react_controls.py`
- `backend/tests/unit/react/test_api_service.py`
- `backend/tests/unit/react/test_persistence.py`
- `backend/tests/integration/react/test_api_service.py`
- `backend/tests/integration/api/test_react_api.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 10 验证

- ReAct 单元、仓储与全部 API 回归：`139 passed`。
- Alembic 临时 SQLite：`upgrade head` → `downgrade 20260723_04` → `upgrade head`，最终 `20260723_05 (head)`。
- Ruff 与 Git diff check 通过；唯一告警为 Windows Pytest 缓存写入，不影响结果。

## Task 10 下一步

- 实施 Task 11：阶段 6 离线 smoke，覆盖成功恢复、未知结果查询、循环/预算终止、审批拒绝和人工接管。

## 阶段 6 Task 11

- 新增 Phase 6 独立离线 harness，以临时 SQLite 运行迁移和持久化受控循环。
- 验证失败经结构化观察、固定分类和确定性计划差异生成新的 `proposed:isolate_endpoint`，随后只通过阶段 5 注册表、策略、离线适配器和验证器完成可信执行。
- 独立覆盖未知变更结果只查询注册状态、重复指纹循环检测、预算耗尽、审批拒绝和人工接管/恢复。
- Phase 6 wrapper 串行执行 Phase 5 前置 smoke，继承 Phase 3/4/5 离线能力验证；不联网、不执行 Shell、不访问真实安全设备。
- 公开轨迹再次检查 tenant、actor、原因摘要、request ID、原始适配器结果、提示和思维链不泄漏。

## Task 11 修改文件

- `tests/scripts/phase6_smoke.py`
- `tests/scripts/run-phase6-smoke.ps1`
- `backend/tests/integration/react/test_phase6_smoke_contract.py`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-23.md`

## Task 11 验证

- Phase 6 smoke 合同测试：`2 passed`。
- 独立临时 SQLite harness 通过。
- 完整 `run-phase6-smoke.ps1` 通过，包含 Phase 3/4/5 前置 smoke；退出码为 0。
- 系统临时目录中 `shieldchain-phase6-smoke-*` 遗留数量为 0。
- `REAL_MODEL_PLANNING_TESTED=False`，`REAL_DEVICE_PATHS_TESTED=False`。

## Task 11 下一步

- 实施 Task 12：把 Phase 6 纳入完整验证脚本，运行迁移往返、后端/前端、RAG、安全、脚本合同和全部 smoke 门禁，并完成阶段出口文档。
