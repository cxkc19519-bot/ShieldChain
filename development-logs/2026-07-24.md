# 2026-07-24 开发日志

## 阶段 6 Task 12

- 完整验证入口由 Phase 5 smoke 升级为 Phase 6 smoke，并把新的包装器纳入 PowerShell AST 安全合同。
- 根目录执行的完整 Pytest 显式使用 `--import-mode=importlib`，避免不同测试目录中同名模块发生导入冲突。
- README、系统架构、本地运维和路线图已同步受控 ReAct 边界、人工接管、离线验证范围及未测试声明。
- 阶段 6 正式完成；阶段 7 完整前端仍未开始。

## Task 12 修改文件

- `scripts/verify.ps1`
- `tests/scripts/run-contract-tests.ps1`
- `backend/pyproject.toml`
- `README.md`
- `docs/architecture/system-design.md`
- `docs/operations/local-development.md`
- `docs/plans/development-roadmap.md`
- `development-logs/2026-07-24.md`

## Task 12 完整验证

- 后端 Ruff：通过。
- 后端完整 Pytest：`985 passed, 1 skipped`；唯一告警为 Windows Pytest 缓存写入，不影响结果。
- 前端 ESLint、TypeScript 和生产构建：通过；Vitest `58 passed`。
- Alembic 临时 SQLite：`upgrade head` → `downgrade base` → `upgrade head`，最终包含 `20260723_05`。
- 固定双语 RAG 评测：`16 passed`。
- PowerShell 脚本合同：`52 passed`。
- Phase 3/4/5/6 离线 smoke：全部通过。
- `shieldchain-phase6-verify-*` 与 `shieldchain-phase6-smoke-*` 临时目录遗留数量均为 0。
- `REAL_MODEL_PLANNING_TESTED=False`，`REAL_DEVICE_PATHS_TESTED=False`。

## 阶段结论

- Phase 6 受控 ReAct 自主闭环达到离线阶段出口条件。
- 未联网、未产生模型费用、未操作真实安全设备、未验证真实模型自主规划。
- 下一阶段为 Phase 7 完整前端；本 Task 不提前实现阶段 7。

## 阶段 7 设计与计划

- 完成完整安全运营前端设计，明确六页信息架构、共享运行上下文、视觉/可访问性、安全投影和支撑 API 原则。
- 拆分 10 个独立 Task：应用壳、运行上下文、总览、调查、智能体/ReAct、知识库、处置、报告审计、全局回归和最终门禁。
- 不使用伪造成功数据；真实模型、云服务和真实设备边界保持不变。

下一步：实施 Task 1 统一应用壳与设计系统。

## 阶段 7 Task 1

- 统一安全运营应用壳，加入产品标识、离线仿真环境标识、工作区导航和明确的安全变更边界。
- 扩展语义化颜色、间距、圆角、阴影和动效令牌，补齐表单、按钮、状态徽标、页面标题及加载/空/错误状态原语。
- 增加跳转主内容链接、可见焦点、当前页语义和 reduced-motion 降级，保持窄屏导航可用。
- 所有文案只描述真实离线能力，不宣称真实设备或云模型已连接。

## Task 1 验证

- ESLint：通过。
- TypeScript：通过。
- 应用壳、设计令牌和状态组件定向 Vitest：`16 passed`；完整前端 Vitest：`60 passed`。
- Vite 生产构建：通过。

下一步：实施 Task 2 共享案件/运行上下文。

## 阶段 7 Task 2

- 新增共享案件/运行上下文，查询参数 `incident_id`/`run_id` 优先于 `sessionStorage`，并在导航间保留。
- 提供应用壳手工替换和清除入口；仅接受 UUID，仅持久化两个业务标识，不存储 tenant、principal、权限、Token 或凭据。
- 调查启动后发布事件与运行标识；携带运行 ID 打开调查页时恢复公开调查投影。
- 智能体与处置页读取共享运行 ID；上下文切换会卸载或中止旧请求，避免陈旧响应覆盖新页面。

## Task 2 验证

- ESLint：通过。
- TypeScript：通过。
- 上下文、应用壳、调查、智能体和处置定向 Vitest：`31 passed`。
- 覆盖 URL 优先、会话回退、非法 ID、手工切换/清除和旧请求取消。
- 完整前端 Vitest：`64 passed`。
- Vite 生产构建：通过。

下一步：实施 Task 3 运营总览。

## 阶段 7 Task 3

- 将总览页从单一健康卡扩展为安全运营概览，继续使用真实健康检查，不新增重复后端聚合模型。
- 按共享运行 ID 加载公开调查投影，展示调查状态、风险等级、可信证据数量、处置结果和验证状态。
- 无运行时明确引导启动或选择调查；失败时不展示陈旧数据并提供重试，卸载或切换时中止请求。
- 不渲染证据原始引用、完整性摘要、工具 before/after state 或其他未列入总览白名单的字段。

## Task 3 验证

- ESLint：通过。
- TypeScript：通过。
- 总览、应用壳和共享状态组件定向 Vitest：`16 passed`。
- 覆盖成功、空状态、失败重试、健康/调查请求取消和敏感字段不渲染。
- 完整前端 Vitest：`68 passed`。
- Vite 生产构建：通过。

下一步：实施 Task 4 事件调查工作台。

## 阶段 7 Task 4

- 重排事件调查页，前置当前事件与终端 → 进程 → 远端攻击路径，并明确展示离线仿真代次。
- 将运行状态、调查阶段、证据链、研判结论、工具结果、验证和审计时间线拆分为可扫描区域。
- 调查启动继续通过共享上下文发布事件/运行 ID；携带已有运行 ID 时沿原公开 API 恢复。
- 保留 normal/fail_block_once 非生产边界、500 ms 轮询、失败不宣称闭环和请求取消行为。

## Task 4 验证

- ESLint：通过。
- TypeScript：通过。
- 调查状态机与信息架构定向 Vitest：`14 passed`；与应用壳联合回归：`24 passed`。
- 覆盖闭环验证、不完整验证、三类终止状态、轮询恢复、重置、失败模式和卸载取消。
- 完整前端 Vitest：`68 passed`。
- Vite 生产构建：通过。

下一步：实施 Task 5 智能体与 ReAct 工作台。

## 阶段 7 Task 5

- 在同一页面并行组合阶段 4 协作轨迹与阶段 6 ReAct 公开轨迹，单侧缺失时显式降级并保留另一侧真实数据。
- 协作区展示共享摘要、预算、角色、结构化交接、原因码和可信引用。
- ReAct 区展示循环预算、观察/失败分类、计划差异、决策、控制记录和安全引用，不展示思维链或私有提示。
- 人工接管/恢复仅向现有服务端控制端点提交操作原因；tenant、principal、权限、状态和修订继续由服务端决定。

## Task 5 验证

- ESLint：通过。
- TypeScript：通过。
- 协作 API、ReAct API、联合页面与应用壳定向 Vitest：`17 passed`。
- 上下文切换请求取消：`1 passed`。
- 覆盖严格公开 Schema、私有形状拒绝、单侧降级、观察/分类/重规划/决策展示和最小人工控制载荷。
- 完整前端 Vitest：`72 passed`。
- Vite 生产构建：通过。

下一步：实施 Task 6 知识库工作台。

## 阶段 7 Task 6

- 将知识库页纳入统一 PageHeader、加载态和空状态组件，公开标题与页面语义保持一致。
- 统一本地上传、文档/版本状态、混合检索、回答、引用、结构化拒答和降级信息的工作流。
- 保留文件格式/大小限制、请求取消和证据不足时不生成答案的失败关闭语义。
- 为已交付页面补齐共享 PageHeader 的 `aria-labelledby` 标题关联。

## Task 6 验证

- ESLint：通过。
- TypeScript：通过。
- 知识库、应用壳与相关工作台定向 Vitest：`38 passed`。
- 覆盖加载到空状态、键盘主路径、服务端敏感字段不渲染、结构化拒答/降级、上传校验和卸载取消。
- 完整前端 Vitest：`75 passed`。
- Vite 生产构建：通过。

下一步：实施 Task 7 处置中心。

## 阶段 7 Task 7

- 重构处置中心，按服务端公开状态与评估风险展示策略、审批、执行尝试、验证、原因码和证据引用。
- 在可信工具公开投影中增加服务端评估的 `risk` 字段，前端不自行分类或提交风险。
- 仅在合法预执行状态显示批准、拒绝、暂停、恢复和取消动作；执行中、验证中与终态不提供伪动作。
- 审批与控制无论成功或失败都会重新拉取可信轨迹，避免并发冲突后继续展示过期状态。
- 紧急停止明确只阻止尚未下发的调用，不宣称撤回执行中动作；所有变更仍仅提交操作原因。
- 补齐统一标题、加载/空/错误状态、移动端布局、焦点样式、请求取消和敏感字段不渲染覆盖。

## Task 7 验证

- 后端 Ruff：通过。
- 可信工具公开 API 集成测试：`2 passed`。
- ESLint：通过。
- TypeScript：通过。
- 处置中心 API、页面与应用壳定向 Vitest：`17 passed`。
- 完整前端 Vitest：`79 passed`。
- Vite 生产构建：通过。

下一步：实施 Task 8 报告与审计。

## 阶段 7 Task 8

- 移除 `/reports` 占位路由，交付只读“报告与审计”工作台。
- 并行组合事件、调查、审计、阶段 4 智能体、阶段 5 可信工具和阶段 6 ReAct 公开投影；单个来源失败不阻断其他来源。
- 直接展示服务端调查状态、研判结论、风险和验证结果，不在前端补写未知结论。
- 按审计序列号稳定排序时间线，只展示事件类型、时间和请求 ID，不渲染审计 payload。
- 提供事件摘要、证据完整性、智能体确认事实、可信工具状态、ReAct 决策和打印友好布局。
- 缺少事件/运行 ID 或任一公开来源时明确标注不可用；共享上下文切换和卸载会取消页面请求。

## Task 8 验证

- ESLint：通过。
- TypeScript：通过。
- 报告聚合、页面与应用路由定向 Vitest：`15 passed`。
- 覆盖部分来源降级、无选择空状态、加载、卸载取消、有序审计和私有 payload 不渲染。
- 完整前端 Vitest：`84 passed`。
- Vite 生产构建：通过。

下一步：实施 Task 9 全局可访问性、安全与响应式回归。

## 阶段 7 Task 9

- 新增路由级失败关闭错误页，异常时停止渲染原页面且不回显异常消息中的敏感内容。
- 客户端路径变化后将焦点移动到主要内容；保留初始焦点和“跳到主要内容”键盘入口。
- 共享加载状态增加 `aria-busy` 与原子播报，错误继续使用 alert 语义。
- 修复处置/报告样式使用的未定义焦点与成功色令牌，并增加全量 CSS 自定义属性引用合同。
- 在 420px 断点将导航、上下文按钮和页头动作改为单列，确保 320px 最小视口不横向挤压。
- 完善减少动画偏好，停止无限状态脉冲并清除过渡延迟。
- 新增渲染组件静态安全合同，禁止服务端 tenant、principal、凭据、私有提示和思维链字段进入 TSX。

## Task 9 验证

- ESLint：通过。
- TypeScript：通过。
- 应用壳、路由焦点、错误边界、共享状态、样式与安全定向 Vitest：`21 passed`。
- 覆盖 320px、桌面壳、键盘焦点、语义/ARIA、减少动画、CSS 变量、失败关闭和敏感字段禁渲染。
- 完整前端 Vitest：`89 passed`。
- Vite 生产构建：通过。

下一步：实施 Task 10 Phase 7 smoke、完整门禁与文档。

## 阶段 7 Task 10

- 新增 Phase 7 跨页离线 smoke，访问运营总览、事件调查、智能体与 ReAct、知识库、处置、报告与审计六个真实路由，并在拒绝全部 `fetch` 的环境中验证模拟边界和敏感字段禁渲染。
- 新增 PowerShell Phase 7 包装脚本，串联阶段 2–6 smoke，隔离并恢复四个实时测试开关，使用自有临时目录并安全清理。
- 将完整验证终点从 Phase 6 提升到 Phase 7，并将脚本合同扩展到 53 项确定性断言。
- 修复完整 smoke 揭示的可信工具风险字段类型回归：公开轨迹继续消费持久化策略行中的字符串风险值。
- 加固仓库就绪测试，在 worktree 中执行只读 Git 查询时仅对解析出的仓库路径传入单次 `safe.directory`，不修改全局配置。
- 更新 README、系统架构、Windows 本地运维和开发路线图，明确公开投影、六工作区与离线验收边界。

## Task 10 完整验证

- Ruff：通过。
- 后端 Pytest：`987 passed, 1 skipped`；仅有不可写 `.pytest_cache` 环境警告。
- 前端 ESLint 与 TypeScript：通过。
- 前端 Vitest：`90 passed`（24 个测试文件）。
- Vite 生产构建：通过。
- Alembic：`upgrade head` → `downgrade base` → `upgrade head` 通过。
- 固定双语 RAG 与安全回归：`16 passed`。
- PowerShell 脚本合同：`53 passed`。
- 阶段 2–7 离线 smoke：通过。
- `NETWORK_ACCESS_TESTED=False`、`REAL_MODEL_PLANNING_TESTED=False`、`REAL_DEVICE_PATHS_TESTED=False`。

阶段 7 完成。下一步：进入阶段 8 加固与比赛交付，优先建立性能、安全、容器化和交付物基线。

## 阶段 8 规划

- 明确阶段 8 目标：把完整离线产品闭环提升为可测量、可部署、可审计、可复现的比赛交付包。
- 将性能/安全基线、HTTP 加固、查询性能、可观测性、容器、CI/供应链、工程文档、PPT、视频和最终打包拆为 10 个独立 Task。
- 继续保持 tenant、审批、可信工具、ReAct 预算和公开字段白名单边界，不用部署便利换取权限放宽。

下一步：实施 Task 1 性能、安全与交付基线。

## 阶段 8 Task 1

- 新增严格版本化的性能预算与报告模型，拒绝未知字段、单位、分位数、样本量和非有限阈值。
- 基线复用真实 FastAPI liveness HTTP 与固定双语 RAG 数据集加载路径，隔离四个实时测试开关并使用安全临时目录。
- 报告仅记录 Python、实现、平台和架构，不记录主机名、用户名、tenant、凭据或请求内容。
- 建立 `delivery/manifest.json`，区分 `available` 与 `planned`，禁止绝对路径和父目录逃逸。
- 固化首份 Phase 8 本地报告和未测边界。

## Task 1 验证

- Ruff：通过。
- 完整后端 Pytest：`995 passed, 1 skipped`；仅有不可写 `.pytest_cache` 环境警告。
- 基线模型、预算、清单和脚本合同：`8 passed`。
- `health_live_http`：25 样本，p50 `1.656 ms`，p95 `2.499 ms`，最大 p95 `100 ms`。
- `rag_dataset_load`：25 样本，p50 `0.099 ms`，p95 `0.114 ms`，最大 p95 `100 ms`。
- `NETWORK_ACCESS_TESTED=False`、`REAL_MODEL_PLANNING_TESTED=False`、`REAL_DEVICE_PATHS_TESTED=False`。

下一步：实施 Task 2 HTTP 与生产配置加固。

## 阶段 8 Task 2

- 新增全局 HTTP 安全中间件：`no-store`、严格 API CSP、Permissions-Policy、Referrer-Policy、`nosniff`、防嵌入和生产 HSTS。
- 新增 Host 白名单与无凭据 CORS 白名单，只开放产品实际使用的方法、请求头和 `X-Request-ID` 响应头。
- 新增全局声明请求体上限与稳定 `400/413` 公开错误，不回显无效长度或请求内容；知识上传继续使用更严格的专用预算。
- 配置严格限定环境、Host、Origin 和请求大小；生产 profile 拒绝通配信任。
- 保留历史 `test→testing` 与大小写归一化，修复全量回归发现的兼容性问题，同时继续拒绝未知环境。
- 更新 `.env.example` 与本地运维文档。

## Task 2 验证

- Ruff：通过。
- 配置与 HTTP 安全定向测试：`19 passed`。
- 环境兼容受影响集合：`51 passed`。
- 完整后端 Pytest：`1007 passed, 1 skipped`；仅有不可写 `.pytest_cache` 环境警告。
- 未执行真实网络、云模型或安全设备调用。

- 阶段 2–7 离线 smoke：通过。
下一步：实施 Task 3 数据库、RAG 与聚合性能。

## 阶段 8 Task 3

- 审计事件、RAG、工具、ReAct 与报告聚合查询，选择两个可由 SQLite 查询计划证明的热路径缺口。
- 为事件详情和最新运行新增 `(incident_id, created_at, id)` 与 `(simulation_instance_id, created_at, id)` 覆盖排序索引。
- 将工具 `(tenant_id, run_id)` 索引扩展为 `(tenant_id, run_id, created_at, id)`，删除冗余前缀索引。
- 新增 `20260724_01` 可逆迁移；降级恢复旧工具索引，升级恢复全部新索引。
- 新增真实 SQLAlchemy 元数据上的 `EXPLAIN QUERY PLAN` 合同，禁止三个热查询使用临时排序 B-tree。
- 固化查询性能加固报告并加入交付清单。

## Task 3 验证

- Ruff：通过。
- 查询计划与迁移往返：`4 passed`。
- 完整后端 Pytest：`1011 passed, 1 skipped`；仅有不可写 `.pytest_cache` 环境警告。
- 未宣称生产并发、其他数据库执行计划或真实云/设备性能。

下一步：实施 Task 4 就绪、版本、日志与关闭。

## 阶段 8 Task 4

- 新增最小公开版本端点，只返回服务名、安装包版本和期望 schema revision。
- readiness 扩展为数据库、Alembic `20260724_01` 与应用接受状态的合取；缺表、旧 revision、连接失败或关闭中均返回 503。
- fresh-checkout 合同更新为未迁移失败关闭，执行 `alembic upgrade head` 后恢复 ready。
- lifespan 在恢复完成后标记接受请求，关闭前先标记 stopping，再调用既有的超时、取消、幂等后台关闭。
- 日志脱敏扩展到 tenant、principal、actor、提示、推理轨迹、原始 payload 和证据 payload，同时保留公开 request ID 与状态元数据。
- 更新架构和 Windows 运维文档，明确三个健康端点职责与关闭语义。

## Task 4 验证

- Ruff：通过。
- 健康、版本、日志与关闭定向测试：`32 passed`。
- fresh-checkout 迁移前后合同：`8 passed`。
- 完整后端 Pytest：`1015 passed, 1 skipped`；仅有不可写 `.pytest_cache` 环境警告。
- 阶段 2–7 离线 smoke：通过。
- `NETWORK_ACCESS_TESTED=False`、`REAL_MODEL_PLANNING_TESTED=False`、`REAL_DEVICE_PATHS_TESTED=False`。

下一步：实施 Task 5 非 root Docker 与 Compose。

## 阶段 8 Task 5

- 新增 Python 多阶段后端镜像，以固定非 root UID 运行 Uvicorn，并保留 Alembic 迁移入口。
- 新增 Node 构建与非 root Nginx 前端镜像，提供 SPA fallback、静态资源缓存、安全响应头、`/healthz` 和 `/api/` 内部反向代理。
- 新增 Compose 拓扑：一次性迁移成功后启动后端，readiness 成功后启动前端；后端不向宿主机发布端口。
- 所有容器使用只读根文件系统、`cap_drop: ALL`、`no-new-privileges` 和最小 `tmpfs`；SQLite 数据使用命名卷。
- 新增 `.dockerignore`，排除版本库元数据、虚拟环境、依赖目录、缓存、测试、文档、交付物、数据库与日志。
- 新增容器静态合同报告和交付清单项。

## Task 5 验证

- Ruff：通过。
- Dockerfile、Nginx、Compose 与 `.dockerignore` 静态合同：`5 passed`。
- 完整后端 Pytest：`1020 passed, 1 skipped`；仅有不可写 `.pytest_cache` 环境警告。
- 前端 TypeScript 与 Vite 生产构建：通过。
- Docker CLI：不可用。
- 镜像构建、Compose 启动、容器健康检查和 registry 标签拉取：未执行。
- `DOCKER_RUNTIME_TESTED=False`。
- `NETWORK_ACCESS_TESTED=False`、`REAL_MODEL_PLANNING_TESTED=False`、`REAL_DEVICE_PATHS_TESTED=False`。

下一步：实施 Task 6 容器 smoke、CI 与供应链门禁。

## 阶段 8 Task 6

- 新增 Python 3.14 生产运行时与 CI 测试精确依赖锁；前端继续使用 npm v3 lockfile 与 registry integrity。
- 新增供应链合同，拒绝未固定 Python 依赖、Git/明文依赖源、缺失 npm SHA-512 integrity、非完整 SHA Actions 和高权限 CI。
- 新增 Phase 8 容器 smoke：静态合同必跑；Docker 可用时以唯一 Compose project 构建、等待、探活、检查 UID/只读根文件系统，并清理自有卷。
- Compose 数据卷改为 project-scoped，避免 smoke 清理命中固定共享卷。
- 新增 Windows 完整质量门禁与 Linux Compose smoke CI；只读权限、禁用 checkout 凭据，三个官方 Actions 固定完整 commit SHA。
- CI 不继承 live 测试开关，不调用 DeepSeek、Embedding、Milvus、Reranker 或真实安全设备。

## Task 6 验证

- Ruff：通过。
- 容器与供应链静态合同：`10 passed`。
- 完整后端 Pytest：`1025 passed, 1 skipped`；仅有不可写 `.pytest_cache` 环境警告。
- PowerShell 脚本合同：`53 passed`。
- Phase 8 容器 smoke：静态合同通过，`DOCKER_RUNTIME_TESTED=False`，原因 `docker_cli_unavailable`。
- `pip check`：`No broken requirements found`。
- CI YAML：成功解析，包含 `windows-quality` 与 `linux-container` 两个 job。
- GitHub Actions 未推送、未远端执行：`CI_RUNTIME_TESTED=False`。
- 镜像构建、Compose 运行、容器 UID/只读根检查：本机未执行。
- `NETWORK_ACCESS_TESTED=False`、`REAL_MODEL_PLANNING_TESTED=False`、`REAL_DEVICE_PATHS_TESTED=False`。

官方 Action 固定版本在实施时按 2026-07-24 的已签名 release commit 选择：checkout v7.0.1、setup-python v7.0.0、setup-node v7.0.0。

下一步：实施 Task 7 工程与测试文档。
