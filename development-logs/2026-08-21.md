# 2026-08-21 开发日志

## 完成事项

- 基于当前七角色 ReAct、上下文工程、只读运营工具、可信工具网关和 RAG 边界，完成《ShieldChain 可安装 Skills 运行时实施方案》。
- 冻结第一版 Skill 包格式、安装来源、两阶段安装流程、不可变版本、角色绑定、运行时选择、上下文注入和权限交集原则。
- 明确第一版禁止执行包内脚本，Skill 不能注册工具、扩大权限、绕过审批或获取凭据。
- 设计后端模块、数据库表、API、前端 Skills 工作台、错误原因码、安全威胁控制、TDD 实施阶段和验收命令。
- 将 Skills 实施方案加入文档中心阅读顺序。

## 修改文件

- `docs/plans/skills-runtime-implementation.md`
- `docs/README.md`
- `development-logs/2026-08-21.md`

## 验证命令与结果

```bash
git diff --check
```

结果：通过，无空白错误。

```bash
python - <<'PY'
# 对两份文档执行 UTF-8、NUL、占位标记和凭据赋值形态检查。
PY
```

结果：

- `docs/plans/skills-runtime-implementation.md`：UTF-8 正常，共 1044 行；
- `docs/README.md`：UTF-8 正常，共 48 行；
- 未发现 NUL、未解释的占位标记或凭据赋值形态。

```bash
sha256sum docs/plans/skills-runtime-implementation.md docs/README.md
```

结果：已真实计算两份文档摘要，用于本次本地校验；摘要不作为未来实现协议的一部分。

本次只修改 Markdown 文档，没有修改运行时代码、依赖、数据库迁移或前端代码，因此未运行后端和前端功能测试。

## 遗留问题

- Skills 运行时代码尚未开始实现。
- GitHub 私有仓库安装、发布者签名、公共 Skill Registry、包内脚本沙箱和标准 MCP 依赖联动不属于第一版。
- 实施前需要团队审阅第一版“禁止执行脚本”和“GitHub 来源必须锁定不可变 ref”的安全边界。

## 风险

- 如果后续为了安装便利放开任意 URL、包内代码执行或自动更新，会显著扩大 SSRF、供应链和远程代码执行风险，必须单独设计和评审。
- Skill 是管理员批准的过程指导，但仍不能成为系统授权来源；后续实现若把 Skill 声明与服务端权限做并集，会破坏现有可信工具安全边界。

## 下一步

- 审阅并冻结本方案。
- 按阶段 1 先写失败测试，再实现 Skill 领域合同、Front Matter 解析和校验器。
