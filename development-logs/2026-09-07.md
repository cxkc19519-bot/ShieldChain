# 2026-09-07 开发日志

## 完成事项

- 增加 NTA 隔离实时 PCAP 回放脚本；回放器与 Suricata 共享一次性 Docker 网络命名空间。
- 增加基于 `--internal` Docker bridge、只读挂载、最小 capability、显式双开关和运行超时的安全边界。
- 增加 PCAP 根目录、文件魔数、大小、速率和循环次数校验。
- 回放完成后生成 Suricata 原始事件、ShieldChain 最小化事件和带输入哈希的运行清单。
- 增加回放器镜像定义、操作文档和单元测试。

## 修改文件

- `scripts/nta/pcap_replay_lab.py`
- `scripts/nta/replay/Dockerfile`
- `scripts/nta/README.md`
- `tests/scripts/test_pcap_replay_lab.py`

## 验证

- 待在服务器现有 Python 环境执行单元测试和 Ruff。
- 待构建回放器镜像后使用合成无害 PCAP 完成隔离 Docker 冒烟测试。

## 风险和下一步

- 不直接使用比赛恶意样本作为首次冒烟输入；先验证内部网络、传感器输出和自动清理。
- 实时链路当前只运行 Suricata；Zeek 行为聚合仍由既有离线流水线提供。
- 演示前需选择短 PCAP、固定速率，并验证告警导入后的前端展示时间窗口。
