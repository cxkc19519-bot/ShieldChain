# 2026-08-24 开发日志

> 记录说明：本日志于 2026-08-28 根据已提交 Git 历史、统一实施方案和测试报告补录，
> 用于合并交接；它不是对当天终端逐行输出的重新生成。

## 完成事项

- 在 `codex/mcp-safety-hardening` 上完成 Task 15 安全审查加固。
- 将 Adapter 外部调用移出长数据库事务，在调用前提交 `executing + lease`，成功后提交 attempt 与 `verifying`。
- 增加无 loop、等待执行和运行中计划的周期恢复；执行结果未知时只查询状态，不自动重放变更动作。
- 实施执行与验证 deadline，分别落为 `UNKNOWN` 和 `INCONCLUSIVE`；审批过期进入 `needs_review`。
- 新增 migration head `20260824_08` 和拒绝丢失 `approval_expired` 审计的降级保护。
- 让全新离线仿真 Adapter 只从持久化成功尝试重建状态，不从旧进程内存或未知尝试猜测。
- 将运营报告前端未知 JSON 改为逐字段运行时公开投影校验，并修正 Task 14 smoke 的 `-StaticOnly` 行为。
- 同步 README、架构、部署、测试、路线图和统一实施方案。

## Git 提交

| 提交 | 内容 |
| --- | --- |
| `c86a5aa` | 可信工具耐久下发 |
| `22270b2` | 中断安全循环恢复 |
| `0668e42` | Adapter deadline 与审批过期 |
| `52e4c83` | 耐久仿真验证状态重建 |
| `0b87a75` | 运营报告运行时校验与 smoke 模式 |
| `3906d13` | 恢复后端 Ruff 门禁 |
| `da61cd1` | 安全加固证据和文档同步 |

`codex/mcp-safety-hardening` 完整包含 `codex/mcp-agent-safety-loop` 的 `9852903`，因此后续只需评审前一个分支。

## 主要修改范围

- `backend/src/shieldchain/tools/`
- `backend/src/shieldchain/react/`
- `backend/src/shieldchain/main.py`
- `backend/alembic/versions/20260824_08_add_approval_expired_category.py`
- `frontend/src/features/operations/api.ts`
- `tests/scripts/run-task14-smoke.ps1`
- 对应后端、前端、迁移和交付合同测试
- `README.md`、`docs/architecture/`、`docs/delivery/`、`docs/plans/`

## 验证

- 后端全量：`1190 passed, 27 skipped`；Ruff 全量通过。
- 前端全量：30 个测试文件、`107 passed`；TypeScript、ESLint、生产构建通过。
- `npm audit --audit-level=moderate`：0 漏洞。
- 临时 SQLite：`upgrade head → downgrade -1 → upgrade head` 通过，最终 head 为 `20260824_08`。
- MCP `2026-07-28` conformance 通过；四个 PowerShell 脚本 AST 解析通过。
- Compose/Nginx/供应链静态合同通过。
- 当前主机没有 Docker CLI，因此未把 Docker runtime smoke 标记为通过。

## 未完成与边界

- 真实身份平台/TLS、真实外部 MCP peer、真实设备 Adapter 和生产遥测闭环未验收。
- Wazuh review case 到案件级可执行计划的目标确认和证据映射未实现。
- 生产环境 REST 写控制在真实管理员 RBAC 完成前保持关闭。

## 后续

- 由项目主导人依据[合并交接说明](../docs/delivery/mcp-safety-hardening-merge-handoff-20260828.md)处理并发分支、冲突和合并后门禁。
- 合并后必须重新运行测试，不能直接沿用本日志中的来源分支结果。
